# 🩺 خطة تطوير: شاشة الزيارة + الروشتة + الإشعارات + ثنائية اللغة + CRM

> ملف جديد بناءً على ملاحظات الاستخدام الفعلي (يونيو 2026). مكمّل لـ
> `IMPROVEMENTS_BACKLOG.md`. مرتّب على مراحل قابلة للتنفيذ والاختبار.

---

## 🔴 مرحلة 1 — إعادة تصميم شاشة الزيارة (Workflow الطبيب)

### المشكلة (من الاستخدام)
- الشاشة طويلة والطبيب بينزل تحت ويلاقي **أكثر من زر Save** (واحد للعلامات/الإكلينيكي، وزر لكل تشخيص، وزر لكل فحص، وزر للمرفقات) — **مش عارف يدوس Save على إيه**.
- مفيش زر **«اكتب روشتة»** من داخل الزيارة (الطبيب مضطر يفتح ملف المريض ويعيد إدخال الوزن).
- الترتيب مش بيعكس تدفّق العيادة الطبيعي.

### تدفّق الطبيب القياسي (اللي لازم الشاشة تعكسه)
1. **يشوف الحالة** (شكوى + علامات حيوية + سياق الزيارات السابقة).
2. **يفحص** (الفحص الإكلينيكي).
3. **يشخّص** (تشخيص — ICD).
4. لو محتاج علاج → **يكتب روشتة**.
5. لو محتاج فحوصات → **يطلب تحاليل/أشعة**.
6. **يحدّد متابعة** (موعد بعد فترة).
7. **ينهي الزيارة**.

### الحل المقترح
- **تبويبات (Stepper) أفقية** بدل التمرير الطويل: «الحالة → الفحص/التشخيص → العلاج (روشتة) → الفحوصات → المتابعة».
- **شريط إجراءات ثابت أسفل/أعلى الشاشة (Sticky action bar)** فيه الأزرار المهمة بوضوح:
  - **حفظ الزيارة** (يحفظ العلامات + الإكلينيكي + الخطة في فورم واحد).
  - **اكتب روشتة** ⭐ (يفتح الروشتة مملوءة: المريض + الطبيب + **الوزن من علامات الزيارة** + **التشخيص النهائي**).
  - **حدّد متابعة** (حجز موعد لاحق — موجود كزر، يتنقل للـ sticky bar).
  - **إنهاء الزيارة**.
- كل قسم (تشخيص/فحص/مرفق) يفضل بزر إضافة خاص به، لكن **مميَّز بصرياً كـ «إضافة عنصر»** (Outline صغير) مقابل **الأزرار الرئيسية** (Primary) في شريط الإجراءات — يختفي اللبس.
- **زر «اكتب روشتة»:** `prescriptions.new?visit_id=&patient_id=&weight=&diagnosis=&diagnosis_code=` يقرأ `visit.vitals.weight_kg` و`visit.final_diagnoses()`. (يتطلب `prescriptions.new` يقبل هذه الباراميترات + ربط `Prescription.visit_id`.)

### تقني
- `record.html`: تقسيم لتبويبات Alpine (`x-data` بحالة `tab`)، + sticky action bar.
- `prescriptions/routes.py new()`: قراءة `weight`/`diagnosis`/`diagnosis_code`/`visit_id` من `request.args` وتعبئة الفورم.
- ربط `Prescription.visit_id` (موجود في الموديل بالفعل) وعرض روشتات الزيارة داخل تبويب العلاج.

---

## 🔴 مرحلة 2 — «الطبيب يكتب أقل» (Smart documentation)

- **عبارات جاهزة (Smart Phrases):** زر «فحص طبيعي» يملأ الفحص الإكلينيكي بنص قياسي قابل للتعديل؛ قوالب شكاوى شائعة (كحة/حرارة/إسهال…).
- **نسخ من الزيارة السابقة (Carry-forward):** زر ينقل الشكوى/الخطة من آخر زيارة (السياق ظاهر بالفعل في كارت «الزيارات السابقة»).
- **قوائم اختيار** للأعراض الشائعة بدل الكتابة الحرة.
- **تلخيص بالـAI** (اختياري، المزوّد المحلي للخصوصية): يقترح ملخّص الخطة/التشخيص من العلامات + الشكوى.
- موجود بالفعل: ICD autocomplete، اقتراح جرعة بالوزن، بحث التحاليل/الأشعة.

---

## 🟡 مرحلة 3 — ثنائية اللغة للتشخيصات/التحاليل/الأشعة

### المشكلة
- التشخيص بيتخزّن كنص واحد `Diagnosis.title` (snapshot) — **مايقلبش عربي/إنجليزي** مع لغة البرنامج.
- التحاليل/الأشعة: الموديل `Investigation` فيه `name_ar` + `name_en` + `display_name()` بالفعل، **لكن** الزيارة بتخزّن `name` snapshot واحد فبيظهر بلغة الإدخال فقط.

### الحل
- **التشخيص:** تخزين `title_ar` + `title_en` (والكود ICD موجود). بحث ICD بيرجّع الاتنين بالفعل → نخزّنهما، والعرض `dx.title(current_lang)`.
- **التحاليل/الأشعة في الزيارة:** تخزين `name_ar` + `name_en` (snapshot للاتنين) أو ربط `investigation_id` واستخدام `display_name(lang)`؛ العرض يقلب باللغة.
- **بيانات مرجعية:** إثراء كتالوج ICD والتحاليل/الأشعة بالاسمين (عربي/إنجليزي) في الـseed.

### تقني (هجرة إضافية آمنة)
- `visit_diagnoses`: + `title_en` (والموجود `title` يبقى العربي/الأساسي).
- `visit_investigations`: + `name_en`.
- `prescription_investigations`: + `name_en` (لتظهر بالروشتة باللغتين).

---

## 🟡 مرحلة 4 — إصلاح الإشعارات والرسائل المنبثقة (Flash)

### المشكلة
- رسائل الـFlash (نجاح/تنبيه) **بتفضل ظاهرة** على كل صفحة بعد الانتقال، **وملهاش زر إغلاق ولا اختفاء تلقائي** — «بتظهر وأنا بنتقل من شاشة لشاشة ولما بدوس عليها مش بتروح».
- جرس الإشعارات (التنبيهات الحيّة) بيفضل بنفس العدّاد لأنه **Live** (TTL 90s) — الضغط بينقل للصفحة لكن التنبيه مايختفيش لحد ما السبب يتحل (فاتورة غير مدفوعة مثلاً).

### الحل
- **Flash:** ✅ (هذه المرحلة) زر إغلاق (×) + **اختفاء تلقائي بعد ~5 ثوانٍ** + إمكانية الإغلاق بالضغط.
- **جرس الإشعارات:** توضيح إنها تنبيهات حيّة (تختفي لما السبب يتحل)، + (لاحقاً) خيار **«تم الاطلاع»** لإخفاء تنبيه مؤقتاً لليوم.

---

## 🟢 مرحلة 5 — مركز خدمة المرضى الموحّد (CRM Hub)

### الموجود حالياً (مبعثر)
- `messages.index` (إعدادات/قوالب)، `messages.roster` (إرسال لقائمة اليوم)، `messages.occasions` (أعياد ميلاد/مناسبات)، `messages.sent` (سجل الإرسال)، `MessageLog` (status: queued/link/sent/failed).

### الناقص (من «اللي فات»)
- **داش بورد إرسال موحّد:** مين اتبعتله إيه، **حسب النوع**، **كام ناجح/كام فاشل** (من `MessageLog.status`)، باليوم/الأسبوع.
- **حد أقصى يومي للإرسال** (daily cap) — إعداد + منع التجاوز + عدّاد متبقّي.
- **نظام طوابير/جدولة الإرسال** (queue) بدل الإرسال الفوري فقط.
- **تاريخ الفحص لكل مريض** (آخر/القادم) + **أعياد الميلاد** في مكان واحد.
- **توحيد القوالب:** إزالة ازدواج `wa_tpl_*` القديمة مقابل `MessageTemplate` (بند مفتوح من قبل).

### الحل — صفحة واحدة «خدمة المرضى» بتبويبات
1. **نظرة عامة (Dashboard):** كروت (مرسل اليوم/ناجح/فاشل/المتبقّي من الحد)، رسم بياني بالنوع، آخر العمليات.
2. **اليوم (Roster):** قائمة مواعيد اليوم + إرسال تأكيد/تذكير (موجود — يتنقل هنا).
3. **المناسبات:** أعياد ميلاد + قوالب (موجود — يتنقل هنا).
4. **السجل (Log):** فلترة بالنوع/الحالة/التاريخ + تصدير.
5. **الإعدادات:** القوالب الموحّدة + الحد اليومي.

### تقني
- `messages` blueprint: صفحة `hub` واحدة بتبويبات تجمّع الموجود.
- `MessageLog`: استعلامات تجميع (count by status / by type / by day).
- إعداد `wa_daily_cap` + فحص قبل الإرسال.
- (اختياري) عمود `kind`/`template_type` على `MessageLog` لتصنيف أدق في الداش بورد.

---

## 🟢 مرحلة 6 — بيانات تجريبية (Demo) أغنى

- زيارات كاملة (علامات + تشخيص + روشتة + تحاليل بنتائج + مرفقات) لعرض التدفّق كاملاً.
- سجل رسائل (`MessageLog`) بحالات مختلفة (ناجح/فاشل/link) لتغذية داش بورد CRM.
- مرضى بأعياد ميلاد قريبة + مواعيد متابعة، لاختبار التذكيرات.

---

## ترتيب التنفيذ المقترح
1. **مرحلة 4** (Flash) — إصلاح سريع ✅ (منفّذ مع هذا الملف).
2. **مرحلة 1** (شاشة الزيارة + زر الروشتة) — أعلى قيمة يومية.
3. **مرحلة 2** (يكتب أقل).
4. **مرحلة 3** (ثنائية اللغة).
5. **مرحلة 5** (CRM Hub).
6. **مرحلة 6** (Demo).

---

## ✅ مرجع: اللي اتعمل (PR #47/#48/#49)
- منصّة شاملة + تطعيمات + مالي + مخزون + تقارير + AI مزوّد/محلي.
- **مخططات RCPCH أوفلاين** (`rcpchgrowth`) + API حساب + ألوان حسب الجنس.
- **لوحة الطبيب اللحظية** (جه/منتظر/دفع) + تحصيل سريع.
- **طباعة:** هوامش لكل جهة + معاينة + سطر حقوق على كل تقرير.
