# 🗺️ ROADMAP — المرجع الواحد (PediaPro / GROWELL CLINIC)

> بيلخّص **كل اللي اتعمل** و**كل اللي ناقص**. آخر تحديث: يوليو 2026 — بعد دمج **PR #82**
> (الكاشير: ورديات + دفع مقسّم + منع حذف الفاتورة، وهوية المنشأة + معالج أول تشغيل).
> لو بتبدأ شات جديد، اقرأ ده الأول.
>
> **قواعد الشغل:** كل بند = PR صغير (بناء → اختبار → لقطة → PR → دمج). الهجرات
> إضافية بس. كل حاجة عربي/إنجليزي + وضع ليلي/نهاري.

---

## ✅ خلص ومدموج في `main`

### الأساس (PR #49 → #58) — ملخّص
- **النمو / الحجز / التطعيمات / الزيارة / المالي / الهوية / التقارير / الاستيراد** —
  كلها شغّالة (تفاصيلها في ملفات الخطط بالأسفل).
- **نظام الفلوس الكامل** (سعر ونسبة لكل دكتور، كاشير، درج، مدى الاستشارة، استرداد).
- **مسار التطعيمات الكامل** (نصيب الطبيب، تحصيل، حجز كخدمة، جرد دفعات، أمبولة متعددة).
- **الزيارة ثنائية اللغة** + عبارات سريعة قابلة للتعديل.

### GAHAR — عناصر الاعتماد العملية (مدموجة)
- موافقة مريض موثّقة بتوقيع ولي الأمر، قائمة مشاكل، مطابقة أدوية، تنبيهات
  حساسية/تداخل، مؤشرات جودة، سجل تدقيق (`ActivityLog`).

### 📞 نظام خدمة عملاء المرضى (CRM) — الدفعة الكبيرة
- **هب موحّد** (#70): مكان واحد لكل رسائل واتساب، توحيد القوالب (خلاص التكرار)،
  قالب واحد لكل نوع، **تلقائي/يدوي + تشغيل/إيقاف لكل نوع**، صور في القوالب.
- **معاينة واتساب حية** + محرّر master-detail + شارات متغيّرات بضغطة.
- **الجدولة + لوحة الإرسال + الحدود** (#71): `scheduled_at`, `dispatch_due()` +
  `flask send-due`, نافذة إرسال + حد يومي، إيقاف لكل مريض، لوحة إحصائيات/فلاتر/طابور.
- **المزوّد WaPilot API v2 حقيقي** (#73): هيدر `token`, `send-message` (JSON) و
  `send-image` (رفع ملف multipart) — الصور المحلية بتتبعت مباشرة.
- **المزوّد Meta Cloud API** للإرسال.
- **استبيان رضا المرضى — التجميع** (#75): موديل `Feedback` + صفحة تقييم عامة
  `/f/<token>` (نجوم طبيب + خدمة + NPS + تعليق)، نوع "استبيان" بيتبعت تلقائي عند
  إنهاء الزيارة (يحترم auto/manual + on/off)، زر إرسال يدوي.
- **استبيان رضا — العرض والتحليل** (#76): نجوم كل طبيب في صفحة الأطباء + لوحة
  تحليلات (CSAT/NPS، توزيع، ترتيب الأطباء، تعليقات، نسبة استجابة).

### تحسينات UI مدموجة
- تبويب التطعيم في الزيارة منظّم بكروت/جداول جرعات (#72).
- كارت "المريض الحالي" متوافق مع الثيم (#74).

---

## 🔨 شغّال دلوقتي / التالي مباشرة

### 1) استقبال واتساب (المزوّدين) — "الاستقبال"  ← بيتبني دلوقتي
حقيقي على **Meta** و **WaPilot v2**، وكل الإعدادات من الواجهة. **النتيجة بتتخزن في
نفس الموديلات** فالنجوم والتحليلات بتفضل شغّالة — بيتغيّر بس **قناة الجمع**.
- **Endpoints عامة (بدون لوجين):** Meta (verify GET + POST)، WaPilot (POST بتوكن
  سري في المسار) → **مُطبِّع موحّد** `{provider, from_phone, text, button_id, media[]}`.
- **إعدادات من الواجهة:** مفتاح تفعيل، سر webhook (توليد + إعادة توليد)، Meta
  verify token، وعرض **روابط الـ webhook جاهزة للنسخ** لكل مزوّد.
- **الموجّه:** يطابق الرقم بالمريض، يسجّل الوارد (`MessageLog direction=in`)،
  يظهر في اللوحة.
- **التقاط تقييم من واتساب:** رد برقم (1–5) أو معرّف زر → يملأ `Feedback` المفتوح.
- **محتاج رابط عام** (نفق Cloudflare/ngrok أو دومين) عشان يستقبل فعليًا؛ المنطق
  يتّختبر دلوقتي بمحاكاة الـ webhook.

### 2) تعديل أسئلة الاستبيان من الواجهة — "نغيّر الأسئلة ونسأل على حاجة معينة"
منشئ استبيان: الأسئلة الأساسية (طبيب/خدمة/NPS) تفضل أساسية (التحليلات معتمدة
عليها) بس **العنوان/التشغيل/الترتيب** يتعدّل؛ + **أسئلة مخصّصة** (نجوم/NPS/نعم-لا/نص)
تتضاف وترتّب. الصفحة العامة تتبني ديناميك، والتحليلات تلخّص المخصّص.

### 3) استقبال — ميديا → ملف المريض (خطوة استقبال لاحقة)
المريض يبعت تحليل/أشعة → تنزيل → مرفق في ملف المريض → **إشعار الطبيب الطالب**،
مع مطابقة ذكية لطلبات الزيارة المفتوحة (`VisitInvestigation`: `requested → resulted`)،
اقتراح بالـAI وتأكيد بشري، وحل تعدّد الأطفال بقائمة تفاعلية أو "وارد غير مربوط".

### 4) إرسال تفاعلي (تحسين)
زر تقييم بضغطة (Meta buttons / WaPilot `send-list`) بجانب الرابط لرفع الاستجابة.

---

## 🎯 مواصفة الجاهزية للتشغيل التجريبي (Pilot) — طلب المستخدم الكامل

> اتسجّلت كلها هنا عشان نرجعلها. الترتيب للتنفيذ يتحدد مع المستخدم.

### 1) مراجعة سير العمل (Workflow)
مراجعة المعمول والمتبقّي، وإعادة تصميم الدورة تبقى سريعة وبسيطة ومنطقية **بدون
تكرار شاشات أو خطوات**.

### 2) صلاحيات الاستقبال (Reception)
موظف الاستقبال **مايشوفش الملف الطبي الكامل**. دوره فقط: تسجيل مرضى · حجز · تسجيل
وصول · تعديل حجز · تحصيل · طباعة إيصالات · عرض بيانات أساسية بس.

### 3) شاشة الطبيب
شبه شاشة الاستقبال باختلاف الصلاحيات: بحث بطبيب/كل الأطباء · عرض المرضى بالترتيب ·
استدعاء المريض · بدء/إنهاء الزيارة · عرض الحالي/القادم/عدد المنتظرين.

### 4) الأداء — تحديث لحظي
إلغاء إعادة تحميل الصفحة بعد كل عملية · **AJAX / live update** · الحفاظ على مكان
التمرير (مايرجّعش لأول الصفحة).

### 5) أنواع الزيارات
كشف جديد · متابعة · استشارة · تطعيم · إجراءات أخرى — حسب إعدادات النظام.

### 6) نظام التطعيم في الزيارة
عند اختيار "تطعيم": يظهر اسم التطعيم + الجرعة + رقم الجرعة **تلقائيًا للطبيب**.

### 7) خدمات أثناء الزيارة → الكاشير
لو الطبيب أضاف تطعيم/إجراء/خدمة أثناء الكشف: النظام يسجّلها · **ينبّه الكاشير** ·
يضيفها تلقائيًا للفاتورة النهائية.

### 8) إعادة تنظيم دورة المحاسبة
حجز → تسجيل وصول → كشف → إضافة خدمات أثناء الزيارة → إنهاء الزيارة → إصدار الفاتورة
→ التحصيل من الكاشير. **تحصيل في مكان واحد** بدل أكتر من شاشة.

### 9) لوحة تحكم الطبيب
عدد المرضى · المنتظرين · الموجودين · المنتهين · غير الحاضرين · ترتيب الدور.

### 10) أولوية الحجز
حجز: أول/ثاني/ثالث الدور أو آخر الدور (الافتراضي) — للحالات الخاصة.

### 11) النسخ الاحتياطي (من الواجهة بالكامل)
تلقائي بفواصل: كل ساعة / 3 / 6 / 12 / يومي / كل 3 أيام / أسبوعي / شهري · نسخة
يدوية · **استعادة من داخل البرنامج** · تحديد عدد النسخ المحتفظ بها · حذف القديم
تلقائيًا · مسار حفظ النسخ.

### 12) سجل العمليات (Audit Log) الكامل لكل مستخدم
اسم المستخدم · IP · الجهاز · نظام التشغيل · المتصفح · وقت الدخول/الخروج · مدة
الجلسة · كل العمليات (تعديل/حذف/إنشاء واستعادة نسخ).

### 13) إعدادات من الواجهة (بدون تعديل ملفات)
إدارة: النسخ الاحتياطية · السجلات · الصلاحيات · الأمان · مسار حفظ النسخ.

### 14) بيانات أولية تلقائية + معالج أول تشغيل (First-Run Wizard)
عند قاعدة جديدة/أول تشغيل: إنشاء أنواع الزيارات · الخدمات الأساسية · أكواد
الخدمات وسيرياﻻتها · **UUID داخلي لكل خدمة** · ربط أنواع الزيارات بالخدمات
(قابل للتعديل من الإعدادات). معالج أول تشغيل ينشئ: التخصص · الخدمات · أنواع
الزيارات · الصلاحيات · الأطباء · الغرف · بيانات تجريبية حسب نوع المنشأة.

### 15) نظام الباقات (Service Packages)
مدير النظام ينشئ باقات (كشف+إيكو، كشف+رسم قلب، كشف+تطعيم، متابعة+تحليل، …). كل
باقة: اسم/كود · الخدمات المرتبطة · السعر العادي · سعر الباقة · الخصومات · فترة
التفعيل. الاستقبال يحجز باقة، والطبيب يضيف باقة أثناء الزيارة، وكل خدماتها تتحسب
تلقائيًا في الفاتورة. تصميم بكيانات مستقلة: **Visit Types · Services · Packages ·
Package Items**.

### الهدف
جاهز للتشغيل التجريبي، بيانات مستقرة، سهل الاستخدام، مرن التخصيص، وقابل للتوسع
(عيادة فردية / مركز / مستشفى متعدد التخصصات) بدون تعديلات برمجية كبيرة.

---

## 🏥 رؤية المنصة الصحية المؤسسية (HEP) — تقييم وأولويات

مقترح "Healthcare Enterprise Platform": نفس الكود يشتغل عيادة/مركز/سلسلة/مستشفى
بالإعدادات بدل التعديل البرمجي. **حوالي 60–70% من الـ Core موجود فعلاً** (ملف طبي،
زيارات، خدمات، محرك تطعيمات، مخازن/batches، مشتريات، مالية+ورديات، تأمين/عقود،
eInvoice، واتساب Cloud API، AI، RBAC+Audit، تقارير، Light/Dark+RTL).

**🟢 يُنفّذ الآن (config-first، مخاطرة قليلة):**
- ✅ **هوية المنشأة + معالج أول تشغيل + تفعيل/تعطيل الموديولز** (PR #82) — `facility.py`
  (7 أنواع منشآت + presets)، `module_enabled` يحرس الـ nav و`module_required`،
  `before_request` يوجّه الأدمن للمعالج لحد ما يتظبط، محرّر لاحق من الإعدادات،
  وخيار تحميل بيانات تجريبية. **يحوّل المشروع من "عيادة أطفال" لمنصة قابلة للتهيئة.**
- ⏭️ أنواع الزيارات/الخدمات/الباقات ككيانات DB تتعدّل من الواجهة (بند pilot #5/#15).
- ⏭️ نتيجة إجراء/فحص عامة على الخدمات (ECG/Echo/Spiro/Audiology) — إدخال يدوي + مرفقات، بدون SDK.
- ⏭️ Device Registry (تعريف/سيريال/ضمان/صيانة/إدخال يدوي) — جدول config، بدون drivers.

**🟡 قريب (v2):** #24 مستخدمون/جلسات، #25 نسخ احتياطية، #26 محرر استبيان،
تقسيط/آجل/دفعة مقدّمة، تمهيد `branch_id`.

**🔴 مؤجّل صراحةً (v3+ — منعاً للـ Scope Creep):** تكامل الأجهزة الفعلي
(SDK/COM/LAN auto-import) + calibration، HL7/FHIR/DICOM، Multi-branch/company كامل،
الـ Builder Engines والـ Workflow/Rule Engine العام، MFA/DR، SaaS/Mobile/Marketplace،
Telemedicine.

---

## 🗂️ مجالات مخطّطة (اتسجّلت — لسه مبدأتش)

### أ. المستخدمون والأمان — "المستخدمين"
- **تتبّع الجلسات:** وقت الدخول، وقت الخروج، **مدة الجلسة**.
- **مصدر الدخول:** الجهاز / user-agent، و**المستخدم داخل منين**.
- **عنوان الـ IP** لكل دخول/جلسة.
- **سجل نشاط لكل مستخدم** (نغوص في أفعال مستخدم واحد؛ `ActivityLog` بيسجّل الفعل و
  الـ IP — نوسّعه ونعرضه لكل مستخدم).
- **مراجعة الصلاحيات ("نبض على الصلاحيات وصلاحيات الشاشات"):** مراجعة الأدوار و
  **صلاحيات الوصول للشاشات/الموديولات**، وجدول واضح لكل دور بيوصل لأنهي شاشات.

### ب. النسخ الاحتياطية والاستعادة — "النسخ الاحتياطية"
- نسخ احتياطي **تلقائي + يدوي** لقاعدة البيانات.
- **استعادة من الواجهة**.
- **جدولة / توقيت** للنسخ التلقائي.
- **الاحتفاظ:** إبقاء آخر N نسخة (قابل للضبط)، وحذف الأقدم.
- قائمة النسخ (وقت/حجم) + تنزيل + استعادة آمنة بتأكيد.

### ج. تلميع الوضع الليلي (Task #14)
- قائمة الإشعارات (`.notif-panel` بتستخدم `var(--surface,#fff)`) وأي أسطح فاتحة
  متبقية في كروت التوب-بار → متوافقة مع الثيم.

### د. إكلينيكي مؤجّل
- **شيت الإيكو** (حقول قياس + مرجعيات + فلاج) حسب تخصص الطبيب.
- **تلخيص الزيارة بالـAI** (المزوّد المحلي).
- **تكرار سنوي/هجري** للجداول الموسمية.

---

## 📁 ملفات الخطط التفصيلية
| الملف | بيغطّي |
|---|---|
| `VACCINE_BILLING_PLAN.md` | نظام الفلوس + التطعيمات |
| `VISIT_CRM_PLAN.md` | الزيارة · ثنائية اللغة · CRM · AI |
| `IDENTITY_DOCTORS_PLAN.md` | الهوية · إدارة الأطباء · GAHAR |
| `IMPROVEMENTS_BACKLOG.md` | الباك‑لوج من الاستخدام اليومي |

## 🚀 التشغيل
```bash
# ويندوز: دبل-كليك start.bat (بيعمل upgrade-db + يفتح المتصفح)
flask --app run upgrade-db   # يدوي
flask --app run send-due     # إرسال الرسائل المجدولة المستحقة
```

## ⛔ موقوف على حاجة منك
1. **بيانات WaPilot** (token + instance name) في الهب عشان الإرسال الحقيقي يشتغل.
2. **رابط عام / نفق** عشان الاستقبال (webhook) يستقبل فعليًا.
3. أولوية: **الاستقبال** vs **المستخدمين/الأمان** vs **النسخ الاحتياطية** vs
   **تعديل الأسئلة** — أنهي الأول بعد الاستقبال.
