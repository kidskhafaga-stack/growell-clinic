# 🔄 خطة التحديث: أطوّر من غير ما العيادة تقف

> «لما أخدت نسخة احتياطية ورجّعتها حصلت مشاكل، وكل مرة بضطر أعمل كل
> الإعدادات من الأول. عايز التحديث يبقى زي أي سوفت وير محترم.»
> يوليو 2026 — الخطة اتكتبت بعد فحص كود النسخ والاسترجاع والتشغيل.

---

## 🔴 السببين الحقيقيين — مفحوصين في الكود مش مفترضين

### ١) الاسترجاع مابيرقّيش قاعدة البيانات بعده

ده أخطر واحد، وعلى الأرجح هو اللي عمل المشاكل اللي حصلت معاك.

`restore_backup()` في `app/utils/backups.py` بيعمل الآتي: ياخد نسخة أمان من
الوضع الحالي، يفك الأرشيف، يحطّ قاعدة البيانات والملفات مكانهم — **وخلاص.
مافيش أي نداء لـ`upgrade-db` بعدها.**

فلو رجّعت نسخة من شهر فات على كود اتطوّر بعدها:

```
الكود الجديد بيدوّر على عمود  cash_accounts.settle_after_days
قاعدة البيانات القديمة       مافيهاش العمود ده
                              ↓
        OperationalError: no such column
```

**والأعراض بتبان متفرّقة** — شاشة بتفتح وشاشة بترمي خطأ، حاجات «ضاعت» وهي
موجودة — فالواحد بيفتكر إن النسخة نفسها بايظة ويبدأ يعيد الإعدادات بإيده.

**الإصلاح:** الاسترجاع ينادي الترقية بنفسه، ويفحص النتيجة قبل ما يقول «تمّ».

### ٢) `start.bat` بيعمل `git pull` كل مرة

سطر ٢٤ في `start.bat`:

```bat
if exist ".git" (
  git pull --ff-only
```

يعني **كل تشغيل للبرنامج هو تحديث** — من غير ما حد يقرّر، ومن غير نسخة أمان
قبله، ومن غير ترقية بعده، وفي نص يوم شغل.

**ولو الـpull جاب كود بيحتاج عمود جديد، العيادة بتقف وهي فاتحة.**

**الإصلاح:** التحديث يبقى **قرار منفصل** — أمر أو زرار — مش أثر جانبي للتشغيل.

---

## 🗂️ الشكل الصح: تلات حاجات منفصلة

```
C:\Growell\
  app\                 ← الكود. ده اللي بيتغيّر في التحديث.
  data\                ← ده اللي عمره ما يتلمس.
    growell.db         ← بيانات العيادة
    uploads\           ← الصور والمرفقات
    backups\           ← النسخ
    clinic.env         ← إعدادات الجهاز ده بالذات
```

**والقاعدة سطر واحد:** التحديث بيستبدل `app` وبس. `data` ماتتلمسش.

وبكده الحالات والمرضى والصور والمخزون والإعدادات — كلهم جوّه قاعدة البيانات —
بيفضلوا زي ما هم. **وده اللي بيحل شكوى «أعيد كل الإعدادات كل مرة».**

> **ملاحظة على الوضع الحالي:** النسخة الاحتياطية **بتحفظ** قاعدة البيانات وكل
> المرفوعات فعلاً (دي شغالة). اللي مش موجود هو `clinic.env` — إعدادات الجهاز
> (المنفذ، مفتاح الجلسة، كلمة سر النسخ). فنقل البرنامج لجهاز تاني بيشتغل
> بنسخة كاملة وإعدادات جهاز جديدة، مش بنسخة لوحدها.

---

## 📦 كل نسخة تعرّف نفسها

المشكلة اللي بتخلّي الاسترجاع لغم: **النسخة مش بتقول هي من أي إصدار.** فملف
جنبها:

```json
{
  "app_version": "1.4.0",
  "schema_version": "2026_07_30_01",
  "created_at": "2026-07-30T14:00:00",
  "has_uploads": true,
  "files_count": 1280
}
```

وبكده:

| الحالة | التصرف |
|---|---|
| نسخة **أقدم** من البرنامج | ✅ ترجع، **وتترقّى تلقائياً**، ورسالة بتقول كده |
| نسخة **نفس** الإصدار | ✅ ترجع عادي |
| نسخة **أحدث** من البرنامج | ⛔ **ترفض** — كود قديم مش هيفهم بياناتها، وهيفسدها |

**والحالة التالتة دي مهمة:** من غير الرفض ده، حد يرجّع نسخة من كمبيوتر
اتحدّث على كمبيوتر ماتحدّثش — والبرنامج يبدأ يشتغل على بيانات مش فاهمها.

---

## 🔁 عملية التحديث

1. البرنامج يدخل **وضع صيانة** (أو يتقفل).
2. **نسخة كاملة تلقائية** باسم `preupgrade`.
3. **يتفحص إن النسخة تتقرا فعلاً** — نسخة أمان مش مضمونة هي مش نسخة أمان.
4. يتحطّ الكود الجديد في `app`.
5. **تجري ترقية قاعدة البيانات.**
6. **فحص سريع:** النظام بيفتح · تسجيل الدخول شغال · المرضى بيتقروا · الصور
   بتتفتح · فاتورة بتتعرض.
7. نجح → الإصدار الجديد يشتغل.
8. فشل → **الكود يرجع للقديم والنسخة `preupgrade` تترجّع.**

**والخطوة ٦ هي الفرق بين تحديث وقمار:** «التحديث خلص من غير خطأ» مش نفس
«البرنامج شغال».

---

## 🧱 والأهم على المدى الطويل: Migrations مرقّمة

الترقية دلوقتي **لستة أعمدة يدوية** في `app/cli.py` — قايمة بتقول «لو العمود
ده مش موجود، ضيفه». ودي ماشية دلوقتي، **وبتبقى هشّة مع التطوير الكبير**: مش
بتعرف تشيل عمود، ولا تغيّر نوعه، ولا تنقل بيانات، ولا **تقول قاعدة البيانات
دي واقفة فين**.

فبالتدريج تتحوّل لـAlembic/Flask-Migrate:

```
001_initial
002_add_invoice_payer
003_add_patient_attachments
004_add_message_templates
```

وبكده أي نسخة بيانات تعرف بالظبط هي عند أنهي خطوة وإيه الناقص للوصول
للإصدار الجديد — **من غير ما حد يعيد إدخال حالات ولا خدمات ولا إعدادات**.

> **بالتدريج مقصودة:** التحويل مرة واحدة معناه أول migration بيحاول يوصف
> قاعدة بيانات موجودة عند عملاء بأشكال مختلفة (كل واحد اتوقف عند نقطة). الأمان
> إن نقطة البداية تتاخد من الوضع الحالي، والجديد بس هو اللي يبقى migrations.

---

## ✅ الترتيب المقترح

| # | الخطوة | ليه هنا |
|---|---|---|
| ~~**١**~~ | ✅ **الاسترجاع بيرقّي قاعدة البيانات بعده** | اتعمل — دي المشكلة اللي حصلت معاك |
| ~~**٢**~~ | ✅ **الـ`git pull` اتشال من `start.bat`** | اتعمل — ومعاه `update.bat` للتحديث المقصود |
| **٣** | **رقم إصدار + `schema_version` في كل نسخة** | من غيره الخطوة ٤ بتخمّن |
| **٤** | **رفض النسخة الأحدث + ترقية النسخة الأقدم** | بيقفل الاتجاهين |
| **٥** | **`clinic.env` جوّه `data` وخارج التحديث** | بيحل «الإعدادات بتضيع» |
| **٦** | **فصل `app` عن `data`** | نقلة مجلدات، محتاجة تجربة |
| **٧** | **Alembic** | آخر واحد، وبالتدريج |

> ✅ **١ و٢ اتعملوا.**
>
> **الاسترجاع:** منطق الترقية كان جوّه أمر `upgrade-db`، يعني **محدّش يقدر
> يشغّله غير واحد قاعد على التيرمينال** — واللحظة اللي محتاجينه فيها أكتر هي
> لحظة محدّش قاعد على تيرمينال فيها. اتنقل لـ`app/utils/schema.py`،
> والأمر والاسترجاع الاتنين بينادوه.
>
> **والترقية دي «الشكل» بس، مش الزرع.** الأمر بيزرع كتالوجات ويعمل backfill —
> وده **مالوش لازمة هنا**: النسخة المسترجعة جايبة بياناتها معاها، وإعادة تشغيل
> الزرع فوق بيانات حد رجّعها بيضيف صفوف ماطلبهاش في اللحظة اللي هو بيحاول
> فيها بالظبط يرجع لحالة معروفة. اللي الاسترجاع محتاجه إن **الشكل** يطابق
> الكود. وبس.
>
> **و`update.bat` اتعمل** لأن شيل الـ`git pull` من غير ما نسيب طريقة تحديث
> كان هينقل المشكلة للي محتاج النسخة الجديدة. الترتيب فيه هو التصميم كله:
> **نسخة احتياطية → كود جديد → مكتبات → ترقية → فحص إن البرنامج بيفتح فعلاً.**
> ولو النسخة الاحتياطية فشلت **بيقف** — لأن التحديث من غير طريق رجوع هو
> بالظبط الحاجة اللي الملف ده موجود عشانها.
>
> **وأمر `backup-now` اتضاف** ومعاه فحص إن النسخة **تتقرا فعلاً**: نسخة محدّش
> فتحها هي **وعد** مش شبكة أمان، واللحظة اللي هتكتشف فيها إنها فاضية هي
> اللحظة اللي كنت محتاجها.

**٣ لـ٧ لسه.** ودول مش بيحلّوا مشكلة قايمة، دول بيمنعوا مشاكل جاية.

---

## ⚠️ حاجتين خد بالك منهم

**١) مفتاح الجلسة وكلمة سر النسخ عمرهم ما يدخلوا Git.** لو `clinic.env` اتحط
في المستودع، أي حد عنده الكود يقدر يفك نسخ العيادة الاحتياطية.

**٢) جرّب التحديث على نسخة من بياناتك، مش على جهاز العيادة.** «اشتغل عندي»
مش دليل — البيانات الحقيقية فيها حالات ماكانتش في بالك، وأول مرة تشوفها
تفضل تكون في تجربة مش في يوم شغل.

---

## ما هي إجابة سؤالك في سطرين

**البرنامج مش محتاج يقف عشان تطوّر.** محتاج إن **الكود والبيانات يبقوا
حاجتين مختلفتين**، وإن **التحديث يبقى قرار ليه خطوات وممكن يرجع**. اللي حصل
معاك مكانش نسخة بايظة — كان استرجاع نجح وترقية مانداهاش حد بعده.
