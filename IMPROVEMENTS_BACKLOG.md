# 📋 سجل التحسينات والتعديلات المطلوبة — GROWELL CLINIC / PediaPro

> ملف متابعة للتعديلات المطلوبة (من الاستخدام الفعلي). انقله لأي سيشن جديد.
> آخر تحديث: يونيو 2026. مكمّل لـ `DEVELOPMENT_PLAN.md`.

---

## 🔴 أولوية عالية (تجربة يومية)

### 0) اختيار المرجع للعيادة كلها، مش لطفل واحد ⚠️

**القيد:** «مرجع جداول التطعيمات» في الإعدادات اختيار **للعيادة كلها**. الطبيب
اللي عايز يمشي على مرجع تاني لطفل واحد بس، مالوش غير إنه يبدّل العيادة كلها.

**تكلفة التبديل — مقاسة، مش تقديرية:**

| التبديل | إيه اللي بيتغيّر |
|---|---|
| `egypt → manufacturer` | **ولا حاجة** — `egypt` مش بيتكلم عن حاجة زيادة |
| `egypt → who` | المكورات الرئوية بس |
| `egypt → cdc` | المكورات الرئوية **+ MenB في ١٦ طفل من ١٨** |

يعني التبديل لبروفايل النشرة آمن. التبديل لـ`cdc` بيشيل MenB من كل طفل تحت
١٦ سنة — وده مش باين من الشاشة.

**اللي بيقفله:** *خطة الطبيب لهذا الطفل* (Clinical Override) على مستوى
`VaccinePlanItem`. الطبيب يفضل على `egypt`، وعند تطعيم واحد بس يختار مصدر
مختلف أو يكتب خطة بنفسه — من غير ما يمس باقي أطفال العيادة.

**شروط بنائه** (متفق عليها):
1. على مستوى الطفل، مش تبديل بروفايل
2. ينتج `effective course_dates` كامل، مش مجرد عدد جرعات
3. يتحل في `course_for` — عشان ملف الطفل والكشف الشامل يشوفوا نفس الخطة **لوحدهم**
4. اختبار تكامل: الملف + `scan_due` + التذكيرات + قوايم الشغل كلهم على نفس الخطة
5. سجل مراجعة: مين، إمتى، المرجع وقت الاعتماد، السبب، المصدر
6. لا يعدّل سجل الجرعات المتاخدة أبداً
7. تحذيرات السلامة (سقف عمر المنتج، الفواصل) تفضل ظاهرة **على الجرعة نفسها**

---

### 0.1) سقف العمر لكل مستحضر — اتعمل جزئياً ✅/⚠️

**اللي حصل:** ملف حقيقي لست عندها ٢٩ سنة، مفيش عندها ولا جرعة مسجلة في
العيادة. شاشة التطعيمات فتحت على **١٩** كورس مقترح، والبانر بتاع «التطعيم
القادم المستحق» قال: السداسي — الجرعة الأولى — **سن شهرين** — بتاريخ **1997**.

**اتعمل ✅**

1. **تاريخ عدّى ومحدش وعد بيه → الصف بيعرض السن ويخفي التاريخ**
   (`stale_projection`). السن هو الجملة اللي الجدول بيقولها فعلاً؛ التاريخ هو
   السن ده مضروب في تاريخ الميلاد. البانر كمان ماينفعش يجاوب بتاريخ عدّى.
2. **سقف العمر المرخّص للمستحضرات اللي نشرتها بتقوله:**
   - كل التركيبات اللي فيها DTaP/DTwP (السداسي، الخماسي، Pentaxim، الثلاثي
     الحكومي المنشّط، والثنائي DT) → **قبل عيد الميلاد السابع**، لأن من ٧
     سنين بيبقى Tdap/Td.
   - Priorix Tetra → **حتى ١٢ سنة شاملة** (ProQuad كان متسجّل كده أصلاً).
3. **رف جديد `closed`** — «فات وقتها — مش مناسبة للسن». قبل كده كانت بتقع تحت
   «لسه بدري عليها»، وده عكس الحقيقة بالظبط.
4. **`manufacturer` بقى المرجع الافتراضي** — «واحنا شغالين على المصنع خليه هو
   الافتراضي». التبديل من `egypt` مقيس: **صفر فرق** على ١٨ طفل و٤٦٨ كورس.

**النتيجة على نفس الملف:** ١٩ جرعة → **١٠**، وكل العشرة دي مرخّصة فعلاً لسن
٢٩ سنة (جدري مائي، إنفلونزا، MenACWY، Bexsero، HPV لحد ٤٥، تيفود). الطفل ٦
شهور و٣ سنين ما اتغيرش عندهم حاجة.

**لسه ناقص ⚠️**

المستحضرات اللي نشرتها **ما بتقولش** سقف عمر لسه من غير سقف، وده صح مؤقتاً:
اختراع رقم عشان الشاشة تبقى مرتبة معناه إن البرنامج بياخد قرار إكلينيكي، والغلط
هنا بيكلّف حد تطعيم. اللي محتاجينه لكل واحد فيهم هو **رقم من مصدر منشور
باسمه** — نفس منطق البروفايلات: الرقم بيتكتب تحت اسم اللي قاله.

مرشحين للمراجعة: MENACWY (Menactra/Menveo لحد ٥٥ سنة)، BCG، OPV/IPV، HBV0،
MMR، MEASLES.

---

### 0.2) سقف الترخيص ≠ سقف نطاق الجدول — اتعمل ✅

اتضاف عمود تاني: `Vaccine.scope_max_age_days`، وحالة جديدة
**«خارج نطاق الجدول»** (`out_of_scope`).

```
VaccineBrand.max_age_final_dose_days  = المستحضر مينفعش يتاخد بعد كده  → «انتهت مدته»
Vaccine.scope_max_age_days            = الجدول ده مبيوصفش أكبر من كده  → «خارج نطاق الجدول»
```

الفرق مش شكلي: CDC بيقول MMR لأي حد ≥١٢ شهر لو مستحق، فلو كتبنا ١٨ في عمود
الترخيص كنا هنقول لواحد عنده ٢٠ سنة «انتهت مدته» وده مش صحيح.

**اللي اتكتب:**

| اللقاح | النطاق | المصدر |
|---|---|---|
| MMR | ١٨ سنة | CDC — Child & Adolescent Immunization Schedule |
| MENACWY | ١٨ سنة | نفسه |

**واللي اتساب فاضي عن قصد — والسبب متكتب في اختبار باسم كل واحد:**

- **BCG** — WHO بيديه كجرعة للرُضّع، ومفيش رقم منشور يصلح كسقف تعويضي.
- **OPV** — رقم «تحت ٥ سنين» مصدره **استهداف الحملات**، مش نطاق التعويضي.
  طفل عنده ٦ سنين ناقص شلل أطفال ده سؤال إكلينيكي حقيقي، و«خارج نطاق الجدول»
  جملة غلط ليه.
- **IPV** — شلل الأطفال **سلسلة واحدة** مع OPV في البرنامج المصري. محرك واحد
  يفهم الاتنين، مش سقفين مستقلين. لحد ما ده يتعمل، مفيش رقم للاتنين.

**قاعدة أمان مهمة: النطاق مش بيلغي وعد.**

كورس اتبدأ في العيادة، أو اتفق عليه الطبيب والأهل، بيفضل بجرعاته الباقية مهما
المريض بعد عن نطاق المرجع. واحد عنده ٢٠ سنة خد MMR الأولى هنا وهو عنده ٥
سنين — الجرعة التانية **لسه مستحقة**. نفس الخط بتاع التواريخ القديمة: نطاق
المرجع بيحكم اللي **بيتقترح**، مش اللي **اتوعد بيه**.

**النتيجة على ملف الست بتاعت الـ٢٩ سنة:**

| | قبل كل ده | بعده |
|---|---|---|
| جرعات معروضة | ١٩ | **٥** (إنفلونزا، جدري مائي، HPV، Bexsero، تيفود) |
| «انتهت مدته» | — | السداسي، الخماسي، Pentaxim، DT، MMRV، الروتا، A |
| «خارج نطاق الجدول» | — | MMR، MENACWY |

**لسه مفتوح ⚠️** محرك سلسلة شلل الأطفال (OPV+IPV كوحدة واحدة) — وهو اللي
هيقفل سقف الاتنين.

---


### 0-ب) المكورات الرئوية على بروفايل `egypt` — ملاحظة

PCV مش في البرنامج القومي، ومفيش مرجع سريري مصري بيحدد جدول تعويضي. فبروفايل
`egypt` **ما بيقولش حاجة** عنه، ونشرة المنتج هي اللي بترد — وهي اللي الطبيب
المصري شغال بيها فعلاً.

الشاشة بقت **بتقول مصدر القاعدة** (شارة على كارت التطعيم)، فالرقم مش بييجي من
العدم. لو ظهر مرجع مصري سريري بيحدد جدول، ده مكانه.


### 1) تدفّق الاستقبال/الكاشير (Reception → Payment → Check-in)
- **المشكلة:** اللي بيحجز هو اللي بياخد الفلوس ويعلّم إن «الحالة جت ودخلت».
- ✅ **لوحة الطبيب اللحظية (تم):** كل طبيب يفتح يلاقي حجوزاته النهارده — **مين جه/منتظر** (حالة الموعد) و**مين دفع/جزئي/لم يدفع/بدون فاتورة** (من فواتير اليوم) + بطاقتي «دفعوا/لم يدفعوا» + زر **تحصيل/إنشاء فاتورة** مباشر من الصف.
- **متبقّي:** تدفّق موحّد في شاشة واحدة (وصل + دفع في خطوة واحدة) — زر «وصل + دفع» يفتح فاتورة سريعة ويحوّل الحالة. ربط `Invoice` بالموعد مباشرة.

### 2) كتابة الروشتة من داخل الزيارة ⭐
- **المشكلة:** الطبيب فاتح الزيارة وأدخل الوزن والقياسات، ومضطر يفتح ملف المريض ويعيد إدخال الوزن لكتابة الروشتة.
- **المطلوب:** زر **«اكتب روشتة»** داخل شاشة الزيارة → يفتح الروشتة **مملوءة مسبقاً**: المريض + الطبيب + **الوزن من علامات الزيارة** + التشخيص (final) من الزيارة.
- **تقني:** `prescriptions.new?visit_id=&patient_id=&weight=&diagnosis=` ويقرأ الوزن من `visit.vitals.weight_kg` والتشخيص النهائي من `visit.final_diagnoses()`. ربط `Prescription.visit_id`.

### 3) فاتورة/إيصال حراري (Thermal) + تحديد ضريبي
- **المطلوب:** **طباعة إيصال حراري** (عرض 58mm/80mm) للدفع، مختصر وسريع.
- **+ تحديد الفاتورة «ضريبية أو لا»** عند الإصدار (موجود `Invoice.is_tax` لكن محتاج يظهر كاختيار واضح + يؤثر على الإيصال/ETA).
- **تقني:** قالب `receipt_thermal.html` بمقاس `@page{size:80mm auto}` + زر طباعة حراري في `invoice_view`. checkbox «فاتورة ضريبية».

### 4) الخصومات المسمّاة — عضوية النادي (مش التأمين بس)
- **المطلوب:** المريض يقدر يظهر **«عضو نادي»** (Club member)، والخصم المسمّى من نوع **عضوية/نادي** (مش شركات تأمين بس).
- **تقني:** علم `is_club_member` (أو فئة عميل جديدة `club`) على المريض/الأسرة + نوع خصم `category` يشمل النادي. الخصم يتطبّق تلقائياً للعضو.

---

## 🟡 أولوية متوسطة

### 5) الداشبورد + صفحة تحليل المرضى
- **المطلوب:** **شيل** من الداشبورد الرئيسي ودجت «كام ذكر/أنثى» وتحليل الأعمار.
- **بدلها:** **صفحة «تحليل المرضى»** منفصلة (Patient Analytics): النوع/الأعمار/الفئات/التوزيع + (نربطها بصفحة `reports.analytics` الموجودة أو نوسّعها).

### 6) تحسين خدمة المرضى / CRM (الجزء ده مش كافٍ)
- **تاريخ الفحص:** «المريض بيعمل الفحص امتى» (آخر/القادم).
- **أعياد الميلاد** (موجود جزئياً في المناسبات — يتحسّن ويتربط).
- **نظام الطوابير** (queue) — تحسين العرض/الترتيب.
- **حد أقصى للإرسال اليومي** (WhatsApp daily cap) — إعداد + منع التجاوز.
- **سجل الإرسال كـ Dashboard:** مين اتبعتله إيه، **حسب النوع**، **كام رسالة ناجحة وكام فاشلة** (من `MessageLog.status`). صفحة إحصائيات للرسائل.

### 7) تكرار في نظام القوالب (Templates) — توحيد
- **المشكلة:** ازدواج بين إعدادات `wa_tpl_*` القديمة في الإعدادات و`MessageTemplate` + `TEMPLATE_DEFAULTS`.
- **المطلوب:** **توحيد كل القوالب في `MessageTemplate`** فقط، وإزالة/ترحيل المفاتيح القديمة من الإعدادات (`wa_tpl_appt_confirm` … إلخ).

### 8) تحكّم أكثر في الروشتة المطبوعة
- ✅ **هوامش لكل جهة (أعلى/يمين/أسفل/يسار) + معاينة مباشرة** لضبط الطباعة على الورق المطبوع مسبقاً (تم).
- ✅ **إظهار/إخفاء الوزن (تم)** — ومعاه الحساسية والأمراض المزمنة وبلوك النمو والتطعيمات،
  كلهم مفاتيح في «العناصر الظاهرة». + **طباعة تجريبية** بداتا متخيّلة لضبط الورق المطبوع.
- متبقّي: التعليمات العامة، رأس مخصّص، ترتيب الأقسام.

### 9) «الطبيب يكتب أقل» — توثيق ذكي
- ✅ **قوالب/عبارات جاهزة (تم):** `app/utils/phrases.py` — عبارات **لكل طبيب** (ليستة العيادة
  هي البداية مش السجن) + **أكواد قصيرة تكتب جُمَل كاملة**: تكتب الكود وتدوس مسافة فتتبدّل
  بالجملة. الصيغة `code|ar|en`.
- **نسخ من الزيارة السابقة (Carry-forward):** نقل الشكوى/الخطة من آخر زيارة.
- **قوائم اختيار** بدل الكتابة للأعراض/الفحص.
- الموجود: ICD/Autocomplete أدوية + اقتراح جرعات AI + (للإضافة) تلخيص الزيارة بالـAI.

---

## 🟢 طبية / مراجع

### 10) مخططات نمو RCPCH (UK-WHO / UK90) — ✅ تم (أوفلاين)
- **الحل:** استخدمنا حزمة **`rcpchgrowth`** الرسمية (pip) — كل حساب z-score/مئين يتم **محلياً على السيرفر** بدون أي API خارجي، فالبيانات ما تخرجش من العيادة.
- **المنفّذ:** مصدر «RCPCH (UK-WHO/UK90)» في قائمة المرجع (2 أسبوع → 20 سنة) للوزن/الطول/محيط الرأس/BMI، بمئينات RCPCH الـ9 الصحيحة **0.4 · 2 · 9 · 25 · 50 · 75 · 91 · 98 · 99.6** (تباعد 2/3 انحراف معياري)، + ألوان حسب الجنس (`themeColors`) + تحديث plugin النقطة الأخيرة ليقرأ لون المريض ديناميكياً.
- **API:** `POST /growth/api/calculate` يستقبل `{gender, age_months, measurement_type, value, source}` ويرجّع `{z, percentile, status}` — يوجّه WHO/CDC لمحرّك LMS المدمج وRCPCH للحزمة، كله أوفلاين.
- **ملاحظة:** ملفات RCPCH PDF المرفوعة كانت صور مخططات فقط (بلا LMS) — الحزمة الرسمية أغنت عنها.

### 11) مطابقة GAHAR (دليل اعتماد المستشفيات)
- **مغطّى:** تعريف المريض، توثيق الزيارة + الطبيب والوقت، ICD، روشتة، تحاليل/نتائج، تطعيمات + رفض، تنبيهات الحساسية، سجل تدقيق (ActivityLog).
- ✅ **التلاتة اللي كانوا مكتوبين «ناقص للاعتماد» — كلهم اتعملوا** (والملف ده كان بيقول
  العكس، وده تاني تصحيح للسطر ده في نفس الشهر):
  - ✅ **توثيق الموافقة المستنيرة:** موديل `Consent` بأنواع (`CONSENT_TYPES`) ونصوص وشاشة طباعة
    (`consent_print.html`). كان موجود من قبل كده.
  - ✅ **قائمة المشاكل النشطة:** موديل `PatientProblem` — عنوان/كود ICD/حالة/تاريخ بداية وانتهاء،
    وظاهرة على ملف المريض وفي التقرير. كانت موجودة من قبل كده.
  - ✅ **قائمة الأدوية الحالية:** `PatientMedication` — اسم/جرعة/تكرار/سبب، ببداية ونهاية، ومين
    سجّلها ومين أوقفها، وداخلة في **فحص التداخلات الدوائية**.
- ✅ **المصالحة الدوائية (تم — أغسطس 2026):** `MedicationReview` — عند كل زيارة، لكل دوا مستمر
  قرار **«استمرار / إيقاف / تعديل»** بيتسجّل باسم اللي قرره وبتاريخ الزيارة.
  **و«استمرار» بيتسجّل زي الباقي عن قصد:** لو اتخزّنت الإيقافات والتعديلات بس، يبقى الدوا اللي حد
  راجعه وقرر يكمّله والدوا اللي محدش بصّ عليه **مالهمش أي فرق في السجل** — والمصالحة أصلاً هي
  الادّعاء إن الليستة كلها اتراجعت.
  ومكانها **الزيارة مش الروشتة**: زيارة مكتبش فيها روشتة لسه ليها ليستة المفروض تتراجع.
- **مطلوب:** إرسال **دليل GAHAR PDF** لعمل **مقارنة بند-ببند** وتنفيذ الفجوات.

---

## ⚙️ نظام/مستخدمين
### 12) كل طبيب بحساب (User) خاص به
- التأكد إن كل طبيب له يوزر منفصل (موجود نظام المستخدمين) — مراجعة أن الحجز/الزيارة/الفاتورة تُنسَب للطبيب الصحيح عبر يوزره.

### 13) بيانات المرضى والعائلات (من الاستخدام — يونيو 2026)
> **فحص الكود أولاً:** اتّضح إن كتير موجود بالفعل — `Parent` فيه `phone`+`phone_alt` (تليفونين)،
> `occupation` (المهنة)، `address`، `is_primary_contact` (الأساسي)، `client_category`. والاستيراد
> بيدعم `reference_number` (الملف الورقي) + تليفون وليّ الأمر + تجميع الإخوة باسم العائلة، بمطابقة
> أعمدة مرنة (aliases عربي/إنجليزي).
- ✅ **بحث العائلة (autocomplete)** في فورم المريض (`/patients/families/search`) بدل القائمة.
- ✅ **تعديل بيانات العائلة** (`/patients/families/<id>/edit`) — الاسم/الإنجليزي/ملاحظات inline.
- ✅ **حقل الجنسية** على وليّ الأمر + إظهار `phone_alt`/`address` في الفورم والعرض (كانت في الموديل بس مش ظاهرة).
- ✅ **تعيين أعمدة تفاعلي عند الاستيراد:** شاشة `import_map.html` — قائمة لكل حقل فيها كل أعمدة
  الملف، مبدئياً على التخمين التلقائي، مع خيار «— تجاهل —». ونفس الشاشة لاستيراد تاريخ الخدمات
  (`history_map.html`). يعني أي ملف مهما كانت رؤوسه.
- ✅ **رقم الملف الورقي = رقم الملف المرفوع:** `reference_number` مدعوم في الاستيراد بالفعل.
- ✅ **اكتشاف الإخوة بالتليفون:** `app/utils/siblings.py` — المطابقة بآخر كلمتين في الاسم **و**
  بتليفونات وليّ الأمر (`phone` + `phone_alt`، بمقارنة آخر ١٠ أرقام علشان `0100…` و`+2010…`
  يتطابقوا). بيقترح ومعاه سبب الاقتراح، والربط بيد الموظّف.
- ✅ **خصم الإخوة (Sibling discount):** `dtype="sibling"` على الخصومات المسمّاة مع `min_siblings`
  و`same_doctor`. الأخ بيتحسب لما تكون زيارته **بفلوس** — الكشف المجاني مش نصّ زوج.

### 14) مدى مخططات النمو WHO/CDC (إجابة سؤال + تنفيذ مقترح)
- **ليه WHO كان 0–5 بس؟** البيانات المدمجة (`standards.json`) فيها **WHO 2006 (0–5)** فقط. WHO عندها
  مرجع تاني **WHO 2007 (5–19)** مش مدمج.
- **الحل المتاح أوفلاين:** حزمة `rcpchgrowth` فيها **WHO كامل (0–19)** و**CDC (0–20)** — متأكَّد منه.
  نقدر نضيف مصدر **«WHO (0–19)»** بنفس أسلوب RCPCH (أوفلاين، بدون API).
- **ليه CDC من 2–20؟** ده **صحيح إكلينيكياً**: CDC نفسها بتوصي باستخدام **WHO تحت سنتين**، فمنحنيات
  CDC بتبدأ من 2. (الحزمة بتوصّل CDC لتحت سنتين عبر WHO داخلياً لو حبينا.)

---

## ✅ اللي اتعمل بالفعل (مرجع سريع — مدمج في main عبر PR #47/#48)
- AI مزوّد قابل للاختيار + **محلي (Ollama/LM Studio) للخصوصية** + قراءة ملف المريض + اقتراح جرعات.
- محرّك حجز شامل + إصلاح الأدمن كطبيب + كارت المريض الحالي.
- تطعيمات (PDF: جداول A/B/C/D، QR وتحقّق، دورة حياة، رفض/تأجيل) + تذكيرات بالقوالب.
- الكشف/الاستشارة: تحاليل/أشعة + نتائج + رفع مستندات + سياق سابق + تبويبات ملف المريض.
- المالي: مصاريف + P&L + الخصومات المسمّاة (حملة/طبيب/فئة/خاص) + أوامر شراء (GRN).
- تقارير تحليلية (تصدير Excel/CSV) + أداء الموظفين + بحث ICD-10 (83 كود).
- الإشعارات (جرس + عدّاد) + جرعة الدواء بالوزن + **مخطط نمو احترافي بمناطق مئينية وأنيميشن**.
- **بانر تنبيه بارز ومتحرّك** على ملف المريض (حساسية / أمراض مزمنة / **تنبيه نمو غير طبيعي**).
- **طباعة الروشتة: هوامش لكل جهة + معاينة مباشرة** لضبط الطباعة على الورق المطبوع مسبقاً.
- **سطر حقوق على كل تقرير مطبوع:** `© Eng. Mohamed Khafaga — All rights reserved` (سطر واحد، يظهر عند الطباعة فقط — يشمل الروشتة).
- **لوحة الطبيب اللحظية:** مين جه/منتظر + مين دفع/متبقّي + تحصيل سريع من الصف.
- **مخططات RCPCH أوفلاين** (`rcpchgrowth`) + API حساب موحّد + ألوان حسب الجنس.

---

## ✅ جولة أغسطس 2026 (PR #253)

> مكتوبة هنا لأن الملف ده اتلقى فيه **تلات بنود مكتوبة «لسه» وهي معمولة** — واللي قراهم كان
> ممكن يبنيهم تاني. فالتسجيل جزء من الشغل مش بعده.

**كتالوج الأدوية**
- **المستحضرات التجميلية اتشالت:** ٢,٩٤٥ صنف (١٢٪ من الكتالوج) بيتخطّوا وقت الزرع — **الملف
  الأصلي ما اتعدلش**، فعيادة ملحوقة بصيدلية تقدر ترجّعهم بتعديل قائمة واحدة. ٢٥,٠٦٥ → ٢٢,١٢٠.
- **أدوية الكبار (~٢,٠٠٠ صنف) فضلت مكانها واتـرتّبت:** الطبيب بيشوف لغاية ١٨ سنة، ومراهق عنده
  سكر محتاج إنسولين. البحث بيرتّب حسب سن الطفل — **اللي الطبيب كتبه بيكسب دايماً**، والسن الأدنى
  المكتوب على المادة الفعالة بيغلب اسم التصنيف، وفوق ١٢ سنة مفيش حاجة بتنزل.

**الروشتة**
- **الوزن** (كل جرعة mg/kg، والورقة كانت شايلة السن بس) + تاريخ القياس لما يكون مش تاريخ الروشتة.
- **الحساسية** — بتتكتب **حتى لو الملف فاضي**: «لا توجد حساسية مسجّلة»، مش «لا يوجد».
- **الأمراض المزمنة** — بتتكتب لما يكون فيه حاجة بس (عكس الحساسية، والفرق مقصود).
- **بلوك النمو** (الطول/محيط الرأس/الكتلة بالمئينات) — **أول عنصر بيبدأ مقفول**، لقوالب الغدد والسكر.
- **مفتاح للتطعيمات** المأخوذة، و**طباعة تجريبية** بداتا متخيّلة وخط عند حد الإزاحة للورق المطبوع.

**خدمات الطبيب** — تعليم اللي بيقدمها فعلاً، في **الزيارة والحجز والكاشير**. الطبيب اللي محدش
علّم له حاجة بيقدّم كل حاجة، ومفيش خدمة بتتخفي — ترتيب مش منع.

**الأدوية المستمرة** — جدول وشاشة، وداخلة في **فحص التداخلات**: طفل ماشي على كاربامازيبين
وتكتبله ماكروليد كان مفيش تحذير خالص. الإيقاف بيسجّل تاريخ ومايمسحش الصف.

**إصلاحات:** تلات أزرار في شاشة الإعداد (405 / تبويب غلط / 403)، شاشة استدعاء الغايبين اللي
مكانش ليها لينك، مفتاح ترجمة ناقص، والقالب الافتراضي اللي كان بيطبع `font-size:Nonepx` وبهامش صفر.

**٢,٥١٧ اختبار بيعدّوا.** الـCI على جيتهاب واقف من ٨ أغسطس (رصيد Actions — ريبو private).
