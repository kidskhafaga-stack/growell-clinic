# 🎨 خطة: هوية البرنامج + ثيمنج العيادة + الأطباء + الإيصال الحراري + التحليلات + GAHAR

> ملف منظّم على أقسام منطقية (يونيو 2026). مكمّل لـ `IMPROVEMENTS_BACKLOG.md`
> و`VISIT_CRM_PLAN.md`. كل قسم مستقل وقابل للتنفيذ كمرحلة.

---

## 🅰️ القسم 1 — هوية البرنامج (PediaPro)
**الهوية المعتمدة:**
- **الاسم:** PediaPro
- **الشعار النصّي:** «حلول طب الأطفال الذكية» / «Smart Pediatrics Care Solution»
- **اللوجو:** رمز الطفل داخل قلب/ورقة، تدرّج **أزرق `#1E7FC2` → أخضر `#5CB646`**.
- **ملفات مطلوبة (يرفعها المستخدم في الريبو):** لوجو أفقي ملوّن، لوجو رمز فقط، نسخة أحادية اللون.
  المقترح: `app/static/img/brand/pediapro-horizontal.png` · `pediapro-mark.png`.

**أين تظهر هوية البرنامج (مع الإبقاء على لوجو العيادة دائماً):**
1. **صفحة الدخول:** لوجو PediaPro + الشعار تحت اسم المنتج (موجود `product_name` — نضيف اللوجو والشعار).
2. **المطبوعات (روشتة/تقارير):** **سطر صغير + لوجو صغير** أسفل الصفحة بجانب سطر الحقوق الموجود
   (`© Eng. Mohamed Khafaga …`). لوجو العيادة يفضل هو الأساسي بالأعلى.
3. **الشريط الجانبي/الفوتر:** اسم + شعار صغير.

**تقني:** مفاتيح `Setting`: `program_logo`, `program_slogan_ar`, `program_slogan_en`
(الاسم `product_name` موجود). إضافة عناصر في `base/login/_flashes`-print-footer + قالب الطباعة.

---

## 🅱️ القسم 2 — ثيمنج من لوجو العيادة (إحساس «بتاعه») مع الإبقاء على هوية PediaPro
- بمجرد رفع **لوجو العيادة**، البرنامج **ياخد لون أساسي (accent)** يطابق العيادة ويلوّن الواجهة به،
  **مع الاحتفاظ بهوية PediaPro** (اللوجو/الاسم/الشعار ثابتة).
- **طريقتان:** (أ) المستخدم يختار لون أساسي يدوياً في الإعدادات (أبسط وأضمن)؛
  (ب) استخراج تلقائي للّون السائد من اللوجو (color extraction) كاقتراح.
- **تقني:** `Setting: clinic_accent` → متغيّر CSS `--green-*`/`--accent` ديناميكي في `base.html`
  (الثيم الحالي أخضر — نخليه قابل للتهيئة). `accent_color` موجود على المستخدم بالفعل (لكل طبيب).

---

## 🅲️ القسم 3 — تايتل الطبيب متعدد الأسطر (عربي/إنجليزي) — ✅ تم
> **منفّذ:** حقلان `print_title_ar` / `print_title_en` (textarea، سطر لكل لقب) في بروفايل الطبيب،
> يظهروا تحت اسمه في الروشتة سطراً سطراً، مع رجوع تلقائي للتخصص لو فاضيين.
**المشكلة:** التايتل الحالي حقول مفردة (`professional_title`, `specialty`, `sub_specialties`).
المطلوب **عدة أسطر حرّة** بالعربي والإنجليزي، سهلة الإضافة. مثال فعلي:
```
د/ احمد جمال قنديل
استشاري طب الأطفال وحديثي الولادة
مستشفى سموحة الجامعي
زميل الكلية الملكية البريطانية لأطباء الأطفال
```
**الحل:** حقلان نصّيان متعدّدا الأسطر `print_title_ar` / `print_title_en` (سطر لكل لقب) في بروفايل
الطبيب — يظهروا تحت الاسم في الروشتة/الطباعة. سهل: textarea + كل سطر يُطبع منفصل.
**تقني:** عمودان TEXT على `users` (هجرة إضافية) + عرض في `prescriptions/view.html` (تحت `doctor_print_name`).

---

## 🅳️ القسم 4 — صفحة إدارة الأطباء + ربط الطبيب باليوزر
**سؤالك «أربط الطبيب باليوزر ولا كعبلة؟»** → **مش كعبلة، وهو أصلاً مربوط:** الطبيب **هو يوزر**
بدور `doctor` (نظام المستخدمين موجود في `/users`). فمفيش جدول أطباء منفصل — وده الأبسط والأصح.
**المطلوب:** **صفحة «الأطباء»** = عرض مُركّز على مستخدمي `role=doctor` فيه: الاسم، التخصص،
التايتل (أسطر)، الترخيص، التوقيع/الختم، قالب الطباعة — مع **إضافة/تعديل سريع**.
**تقني:** صفحة في بلوبرنت `users` تفلتر `role=doctor` وتعرض/تعدّل حقول البروفايل الطبية في مكان واحد.

---

## 🅴️ القسم 5 — الإيصال الحراري (Thermal 80mm) — ✅ تم
**التصميم المطلوب:**
- **لوجو العيادة في المنتصف وكبير** بالأعلى.
- تحته **شعار PediaPro صغير**.
- بيانات الإيصال (رقم/تاريخ/أصناف/إجمالي/مدفوع) مختصرة.
- **نص أسفل قابل للتخصيص** يكتبه الطبيب (شكر/تعليمات/مواعيد) — في **تبويب إعدادات خاص بالطباعة الحرارية**.
- علم **«فاتورة ضريبية أو لا»** (موجود `Invoice.is_tax`).
**تقني:** قالب `receipt_thermal.html` بمقاس `@page{size:80mm auto}` + إعدادات
`thermal_footer_text`, `thermal_show_program_logo` + زر «طباعة حرارية» في عرض الفاتورة.

---

## 🅵️ القسم 6 — صفحة تحليل المرضى (Patient Analytics)
**سؤالك «هل اتلغى تصنيف المرضى من شاشة [الداشبورد]؟»** → **لسه لأ** — التصنيف (نوع/أعمار) **لسه على
الداشبورد**. المطلوب:
- **شيله من الداشبورد** → **صفحة إحصائية مستقلة** تضم: النوع/الأعمار/الفئات + **أهم التشخيصات خلال
  فترة** + اتجاهات (مرضى جدد، زيارات، إيرادات) — مع فلترة بالفترة وتصدير.
**تقني:** نوسّع `reports.analytics` الموجودة أو صفحة جديدة `patients/analytics`؛ نقل widgets من
`main/dashboard.html`؛ تجميع التشخيصات من `visit_diagnoses` خلال الفترة.

---

## 🅶️ القسم 7 — خصم الإخوة (مؤجَّل كعرض مؤقت — Optional)
- **قرارك:** **منعملوش دلوقتي**، لكن نخليه **خيار عرض محدد بمدة** (date-from / date-to).
- **التصميم عند التنفيذ:** خصم مسمّى نوع «عرض/حملة» بنسبة (مثلاً 50%) + شرط «نفس العائلة في نفس
  اليوم/الزيارة» + **مدة سريان** — يتفعّل تلقائياً ضمن المدة فقط.
- **تقني:** `NamedDiscount` فيه أنواع بالفعل — نضيف `valid_from`/`valid_to` + شرط family/visit.

---

## 🅷️ القسم 8 — متطلبات GAHAR (التطوير القادم)
- **مغطّى:** تعريف المريض، توثيق الزيارة + الطبيب والوقت، ICD، روشتة، تحاليل/نتائج، تطعيمات + رفض،
  تنبيهات الحساسية (بانر بارز)، سجل تدقيق.
- **ناقص للاعتماد:** **الموافقة المستنيرة (Consent)**، **قائمة المشاكل النشطة (Problem List)**،
  **المصالحة الدوائية (Medication Reconciliation)**.
- **مطلوب منك:** **دليل GAHAR PDF** لعمل **مقارنة بند-ببند** وتنفيذ الفجوات.

---

## ✅ ترتيب التنفيذ المنطقي المقترح
1. **القسم 3** (تايتل الطبيب متعدد الأسطر) — صغير وأثره فوري على الروشتة.
2. **القسم 1** (هوية PediaPro في الدخول/المطبوعات) — يحتاج رفع ملفات اللوجو.
3. **القسم 4** (صفحة إدارة الأطباء).
4. **القسم 5** (الإيصال الحراري).
5. **القسم 6** (صفحة تحليل المرضى + شيل من الداشبورد).
6. **القسم 2** (ثيمنج العيادة).
7. **القسم 7** (خصم الإخوة كعرض مؤقت) — لاحقاً.
8. **القسم 8** (GAHAR) — عند توفّر الدليل.

## ❓ محتاج منك
- **ملفات لوجو PediaPro** توضع في `app/static/img/brand/` (شفت الهوية بس مش عندي الملفات).
- **دليل GAHAR PDF** للقسم 8.
- **اختبار شاشة الطبيب:** الشاشة (لوحة المواعيد + شاشة الزيارة) **مدموجة في main** — تقدر تجرّبها
  بتشغيل البرنامج محلياً (`flask run`). أقدر كمان أبعتلك معاينة HTML لو حابب.
