# 🔐 مراجعة أمنية — GROWELL CLINIC / PediaPro

> مراجعة على **الكود نفسه** مش على الخطط. يوليو 2026.
> كل بند فيه: إيه المشكلة، إزاي تتستغل، وإيه الإصلاح.
> البنود مرتّبة **بالخطورة الحقيقية على عيادة شغالة**، مش بعددها.

---

## 🔴 حرِج — لازم يتعمل قبل أي تشغيل على شبكة

### 1) البرنامج بيشتغل بوضع التطوير (DEBUG) على كل الشبكة

`start.bat` و`start.sh` بينادوا `python run.py`، و`run.py` بيقرا:

```python
app = create_app(os.environ.get("FLASK_CONFIG", "default"))   # default = DevelopmentConfig
...
app.run(host="0.0.0.0", port=port)
```

و`DevelopmentConfig.DEBUG = True`.

**يعني إيه:** أي خطأ في البرنامج بيفتح **كونسول Werkzeug** في المتصفح. الكونسول
ده بيشغّل بايثون على الجهاز. و`host="0.0.0.0"` معناها إنه مفتوح على **كل
أجهزة الشبكة** مش على الجهاز بس.

**إزاي تتستغل:** أي حد على شبكة العيادة (واي فاي الانتظار مثلاً) يفتح صفحة
بتعمل خطأ → يوصل للكونسول → **ينفّذ أوامر على كمبيوتر العيادة**. ده مش تسريب
بيانات، ده الجهاز كله.

**كمان:** خادم التطوير بتاع Werkzeug **مش معمول للتشغيل الحقيقي** — بينهار
تحت الضغط ومفيهوش تقوية.

**الإصلاح:**
- `FLASK_CONFIG=production` كافتراضي في `start.bat`/`start.sh` و`clinic.env`.
- التشغيل بـ **waitress** (بيشتغل على ويندوز كويس) بدل `app.run`.
- لو التشغيل على جهاز واحد بس: `host="127.0.0.1"`.

---

### 2) مفتاح الجلسات (SECRET_KEY) معروف للجميع

```python
SECRET_KEY = os.environ.get("SECRET_KEY", "growell-clinic-dev-secret-change-me")
```

وفي `clinic.env` السطر ده **متعلّق عليه تعليق**:

```
# SECRET_KEY=
```

**يعني إيه:** أي عيادة شغّلت البرنامج زي ما هو بتستعمل نفس المفتاح — والمفتاح
مكتوب في الكود المفتوح.

**إزاي تتستغل:** المفتاح ده هو اللي بيوقّع كوكي الجلسة. اللي يعرفه **يزوّر كوكي
لأي مستخدم** — يدخل كأدمن من غير باسورد. مش محتاج يخترق حاجة، محتاج يقرا
الكود بس.

**الإصلاح:** يتولّد مفتاح عشوائي **أول مرة** ويتكتب في `clinic.env` تلقائي
(نفس أسلوب `ensure_file`)، والبرنامج **يرفض يشتغل** في وضع الإنتاج لو المفتاح
لسه الافتراضي.

---

### 3) مفيش حماية CSRF خالص

`CSRFProtect` مش متسجّل في `create_app` أصلاً. (`WTF_CSRF_ENABLED = False`
موجود في `TestingConfig` بس — وده بيوحي إن في حماية، وهي مش موجودة.)

**يعني إيه:** كل `POST` في البرنامج ممكن يتزوّر من موقع تاني.

**إزاي تتستغل:** موظف داخل بحسابه يفتح أي صفحة على النت فيها:

```html
<form action="http://clinic-pc:5000/finance/invoices/12/refund" method="post">
  <input name="amount" value="5000">
</form>
<script>document.forms[0].submit()</script>
```

→ **استرداد ٥٠٠٠ جنيه** من غير ما الموظف يعرف. نفس الحكاية مع حذف مريض،
تغيير صلاحيات، فتح فترة محاسبية مقفولة.

**الإصلاح:** `CSRFProtect(app)` + `{{ csrf_token() }}` في كل فورم (أو
`CSRFProtect` مع `<meta>` + هيدر للطلبات من Alpine). **استثناء صريح** لمسارات
الـ webhook العامة — دي بتتحمي بحاجة تانية (بند ٤).

---

### 4) webhook بتاع Meta مفتوح لأي حد

```python
@webhooks_bp.route("/meta", methods=["POST"])
def meta_receive():
    if not _enabled():
        return "", 200
    payload = request.get_json(silent=True) or {}      # ← مفيش أي تحقّق
```

الـ GET (التحقّق) بيتأكد من التوكن. الـ **POST** — اللي بيجيب الرسايل
الحقيقية — **مابيتأكدش من حاجة**. (WaPilot محمي بسر في المسار، Meta لأ.)

**إزاي تتستغل:** أي حد يعرف العنوان يبعت payload مزيّف:
- **تقييمات وهمية** — يرفع أو ينزّل تقييم أي دكتور (`Feedback` بيتملى من
  الوارد).
- **مرفقات على ملفات المرضى** — يحقن صور/ملفات في ملف طفل حقيقي، ودلوقتي
  كمان **بتتربط بطلبات أشعة/تحاليل** (بعد #207/#208). يعني ممكن يحط صورة
  غلط على طلب أشعة طفل.
- **تحميل من روابط** — `wa_media.download` بينزّل من الميديا اللي في الـ
  payload → ضغط على الشبكة، وممكن استكشاف داخلي (SSRF).

**الإصلاح:** Meta بتوقّع كل طلب بهيدر `X-Hub-Signature-256` = HMAC-SHA256
بالـ App Secret. التحقق منه **٦ أسطر**:

```python
import hmac, hashlib
expected = "sha256=" + hmac.new(app_secret.encode(), request.get_data(),
                                hashlib.sha256).hexdigest()
if not hmac.compare_digest(expected, request.headers.get("X-Hub-Signature-256", "")):
    abort(403)
```

(`compare_digest` مش `==` — المقارنة العادية بتسرّب التوقيع بالتوقيت.)

---

## 🟡 مهم — قبل ما العيادة تكبر

### 5) مفيش حد لعدد الطلبات (Rate limiting)

مفيش `Flask-Limiter` ولا أي مكافئ. المسارات المكشوفة:
- `/wa/webhook/meta` و`/wa/webhook/wapilot/<secret>` — قصف بالطلبات.
- `/login` — فيه **قفل بعد ٥ محاولات فاشلة لكل اسم مستخدم** (كويس)، بس مفيش
  حد **لكل IP** — يعني تجربة ١٠٠٠ اسم مستخدم مختلف مافيش حاجة بتوقّفها.
- `/f/<token>` (الاستبيان) — عام.

**الإصلاح:** `Flask-Limiter` بحدود متواضعة على العام، وحد لكل IP على الدخول.

### 6) النسخة الاحتياطية فيها كل بيانات المرضى — من غير تشفير

النسخة بقت `.zip` فيه قاعدة البيانات **وصور المرضى والمستندات**. لو اتنسخت
على فلاشة أو اتبعتت على واتساب، دي كل بيانات العيادة.

**الإصلاح:** خيار **كلمة سر للنسخة** (zip مشفّر أو `age`/GPG)، وتحذير مكتوب
عند التنزيل.

### 7) الكوكي الآمن بس في وضع الإنتاج

`SESSION_COOKIE_SECURE = True` موجود في `ProductionConfig` بس — واللي بيشتغل
`default` (بند ١) مابياخدهاش. لو البرنامج اتحطّ ورا HTTPS، الكوكي هيتبعت على
HTTP برضه.

مفيش كمان: `SESSION_COOKIE_SAMESITE` موجود (`Lax` ✅)، بس ناقص
`REMEMBER_COOKIE_HTTPONLY` و`REMEMBER_COOKIE_SECURE` لكوكي «افتكرني».

### 8) رفع الملفات من داخل البرنامج

`wa_media` (الوارد من واتساب) **متشدّد كويس**: سقف ٢٠ ميجا، وقائمة MIME
مقفولة، والامتداد بيتحدد من الـ MIME مش من الاسم. ✅

بس `save_document` (الرفع من داخل الشاشات) يستاهل نفس المراجعة: تأكيد إن
الامتداد بيتاخد من المحتوى، وإن مفيش `.html`/`.svg` بيتكتبوا في مجلد
بيتقدّم من الويب (SVG فيه جافاسكريبت → XSS على نفس الدومين).

---

## 🟢 كويس فعلاً — متسجّل علشان مايتكسرش

| البند | الحالة |
|---|---|
| الباسوردات | `werkzeug.generate_password_hash` (مش MD5/SHA خام) ✅ |
| SQL Injection | كل الاستعلامات عبر SQLAlchemy ORM — مفيش SQL نصّي بمدخلات ✅ |
| قفل الدخول | ٥ محاولات / ١٥ دقيقة، قابل للتعديل ✅ |
| `_is_safe_next` | بيمنع إعادة التوجيه لموقع خارجي بعد الدخول ✅ |
| الصلاحيات | `module_required` + `capability_required` على كل مسار، والاستقبال بيحصّل من غير ما يشوف الحسابات ✅ |
| سجل التدقيق | `ActivityLog` على كل عملية مؤثّرة ✅ |
| حقن الأوامر في الـ AI | `AI_SYSTEM` متشدّد، والوارد بيتسلّم **كبيانات** مش كتعليمات ✅ |
| الفترات المحاسبية | الشهر المقفول بيرفض الفلوس **وحركات المخزن** ✅ |
| مسار الملفات في الاستعادة | فيه حارس ضد `../` (path escape) ✅ |
| الذكاء الاصطناعي المحلي | خيار Ollama/LM Studio — البيانات ماتخرجش من العيادة ✅ |

---

## الترتيب المقترح للتنفيذ

| # | البند | الحجم | الأثر |
|---|---|---|---|
| 1 | SECRET_KEY يتولّد ويترفض الافتراضي | صغير | يقفل تزوير الجلسات |
| 2 | `FLASK_CONFIG=production` + waitress | صغير | يقفل كونسول الديباج |
| 3 | توقيع webhook بتاع Meta | صغير | يقفل حقن الوارد |
| 4 | CSRF على كل الفورمات | **متوسط** (كل القوالب) | يقفل التزوير من بره |
| 5 | Rate limiting | صغير | يقلّل القصف |
| 6 | تشفير النسخة الاحتياطية | متوسط | يحمي البيانات بره الجهاز |

**١، ٢، ٣ مع بعض أقل من جلسة شغل — ودول اللي بيقفلوا الأبواب المفتوحة على
الآخر.** بند ٤ أكبر لأنه بيلمس كل فورم في البرنامج.

---

> **ملاحظة على المنهج:** المراجعة دي على الكود المدموج في `main` لحد
> PR #208. مافيهاش فحص اختراق فعلي — دي قراءة كود ومسارات، والبنود الحرجة
> اتأكّدت بقراءة الملفات المذكورة سطر بسطر.
