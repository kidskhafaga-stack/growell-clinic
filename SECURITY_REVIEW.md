# 🔐 مراجعة أمنية — GROWELL CLINIC / PediaPro

> مراجعة على **الكود نفسه** مش على الخطط. يوليو 2026.
> كل بند فيه: إيه المشكلة، إزاي تتستغل، وإيه الإصلاح.
> البنود مرتّبة **بالخطورة الحقيقية على عيادة شغالة**، مش بعددها.

---

> ✅ **الأربع بنود الحرجة اتعملوا كلهم** (١ و٢ و٣ و٤) — شوف «اللي اتقفل» تحت.
> ✅ **ومن المتوسط: ٥ (حد الطلبات) و٧ (كوكي «افتكرني») و٨ (رفع الملفات).**
> **الفاضل بند واحد: ٦ — تشفير النسخة الاحتياطية.**

## 🔴 حرِج — لازم يتعمل قبل أي تشغيل على شبكة

### 1) البرنامج بيشتغل بوضع التطوير (DEBUG) على كل الشبكة

`start.bat` و`start.sh` بينادوا `python run.py`، و`run.py` بيقرا:

```python
app = create_app(os.environ.get("FLASK_CONFIG", "default"))   # default = DevelopmentConfig
...
app.run(host="0.0.0.0", port=port)
```

و`DevelopmentConfig.DEBUG = True`.

**يعني إيه:** أي خطأ في البرنامج بيفتح **كونسول Werkzeug** في المتصفح. الكونسول
ده بيشغّل بايثون على الجهاز. و`host="0.0.0.0"` معناها إنه مفتوح على **كل
أجهزة الشبكة** مش على الجهاز بس.

**إزاي تتستغل:** أي حد على شبكة العيادة (واي فاي الانتظار مثلاً) يفتح صفحة
بتعمل خطأ → يوصل للكونسول → **ينفّذ أوامر على كمبيوتر العيادة**. ده مش تسريب
بيانات، ده الجهاز كله.

**كمان:** خادم التطوير بتاع Werkzeug **مش معمول للتشغيل الحقيقي** — بينهار
تحت الضغط ومفيهوش تقوية.

**الإصلاح:**
- `FLASK_CONFIG=production` كافتراضي في `start.bat`/`start.sh` و`clinic.env`.
- التشغيل بـ **waitress** (بيشتغل على ويندوز كويس) بدل `app.run`.
- لو التشغيل على جهاز واحد بس: `host="127.0.0.1"`.

---

### 2) مفتاح الجلسات (SECRET_KEY) معروف للجميع

```python
SECRET_KEY = os.environ.get("SECRET_KEY", "growell-clinic-dev-secret-change-me")
```

وفي `clinic.env` السطر ده **متعلّق عليه تعليق**:

```
# SECRET_KEY=
```

**يعني إيه:** أي عيادة شغّلت البرنامج زي ما هو بتستعمل نفس المفتاح — والمفتاح
مكتوب في الكود المفتوح.

**إزاي تتستغل:** المفتاح ده هو اللي بيوقّع كوكي الجلسة. اللي يعرفه **يزوّر كوكي
لأي مستخدم** — يدخل كأدمن من غير باسورد. مش محتاج يخترق حاجة، محتاج يقرا
الكود بس.

**الإصلاح:** يتولّد مفتاح عشوائي **أول مرة** ويتكتب في `clinic.env` تلقائي
(نفس أسلوب `ensure_file`)، والبرنامج **يرفض يشتغل** في وضع الإنتاج لو المفتاح
لسه الافتراضي.

---

### 3) مفيش حماية CSRF خالص

`CSRFProtect` مش متسجّل في `create_app` أصلاً. (`WTF_CSRF_ENABLED = False`
موجود في `TestingConfig` بس — وده بيوحي إن في حماية، وهي مش موجودة.)

**يعني إيه:** كل `POST` في البرنامج ممكن يتزوّر من موقع تاني.

**إزاي تتستغل:** موظف داخل بحسابه يفتح أي صفحة على النت فيها:

```html
<form action="http://clinic-pc:5000/finance/invoices/12/refund" method="post">
  <input name="amount" value="5000">
</form>
<script>document.forms[0].submit()</script>
```

→ **استرداد ٥٠٠٠ جنيه** من غير ما الموظف يعرف. نفس الحكاية مع حذف مريض،
تغيير صلاحيات، فتح فترة محاسبية مقفولة.

**الإصلاح:** `CSRFProtect(app)` + `{{ csrf_token() }}` في كل فورم (أو
`CSRFProtect` مع `<meta>` + هيدر للطلبات من Alpine). **استثناء صريح** لمسارات
الـ webhook العامة — دي بتتحمي بحاجة تانية (بند ٤).

---

### 4) webhook بتاع Meta مفتوح لأي حد

```python
@webhooks_bp.route("/meta", methods=["POST"])
def meta_receive():
    if not _enabled():
        return "", 200
    payload = request.get_json(silent=True) or {}      # ← مفيش أي تحقّق
```

الـ GET (التحقّق) بيتأكد من التوكن. الـ **POST** — اللي بيجيب الرسايل
الحقيقية — **مابيتأكدش من حاجة**. (WaPilot محمي بسر في المسار، Meta لأ.)

**إزاي تتستغل:** أي حد يعرف العنوان يبعت payload مزيّف:
- **تقييمات وهمية** — يرفع أو ينزّل تقييم أي دكتور (`Feedback` بيتملى من
  الوارد).
- **مرفقات على ملفات المرضى** — يحقن صور/ملفات في ملف طفل حقيقي، ودلوقتي
  كمان **بتتربط بطلبات أشعة/تحاليل** (بعد #207/#208). يعني ممكن يحط صورة
  غلط على طلب أشعة طفل.
- **تحميل من روابط** — `wa_media.download` بينزّل من الميديا اللي في الـ
  payload → ضغط على الشبكة، وممكن استكشاف داخلي (SSRF).

**الإصلاح:** Meta بتوقّع كل طلب بهيدر `X-Hub-Signature-256` = HMAC-SHA256
بالـ App Secret. التحقق منه **٦ أسطر**:

```python
import hmac, hashlib
expected = "sha256=" + hmac.new(app_secret.encode(), request.get_data(),
                                hashlib.sha256).hexdigest()
if not hmac.compare_digest(expected, request.headers.get("X-Hub-Signature-256", "")):
    abort(403)
```

(`compare_digest` مش `==` — المقارنة العادية بتسرّب التوقيع بالتوقيت.)

---

## 🟡 مهم — قبل ما العيادة تكبر

### 5) مفيش حد لعدد الطلبات (Rate limiting) — ✅ اتعمل

**المشكلة كانت:** قفل الدخول بيعدّ الفشل **لكل اسم مستخدم**. تجربة ١٠٠٠ اسم
مختلف من نفس الجهاز مافيش حاجة كانت بتوقّفها — كل اسم فيهم على أول محاولة.

**اللي بقى موجود:** سقف تاني بيعدّ **لكل متصل** مش لكل حساب
(`app/utils/rate_limit.py`)، على التلات أبواب المفتوحة:

| الباب | السقف/دقيقة | ليه الرقم ده |
|---|---|---|
| `/login` | ١٠ | موظف بيغلط في الباسورد مرتين وبيظبطها = ٣ طلبات. عشرة بتسع مكتب فيه أكتر من واحد، وبتحوّل تخمين الباسوردات من آلاف في الساعة لستمية |
| الويبهوك | ١٢٠ | Meta بتبعت دفعات وبتعيد المحاولة. عالي بقصد: إن رسالة مريض تقع عشان نبطّئ مهاجم **التوقيع أصلاً بيرفضه** هي مقايضة غلط |
| `/f/<token>` | ٢٠ | العيلة بتفتح لينكها مرة. عشرين بتغطّي ريفريش ودوسة مزدوجة وموبايل مشترك |

**وحاجات مقصودة في التنفيذ:**

- **الرفض مابيقولش حاجة.** «محاولات كتير» على شاشة الدخول بتقول إن الاسم
  ده موجود فعلاً. الرد ده لازم يبقى واحد للتخمين وللتزوير وللكشط.
- **فتح صفحة الدخول مش محاولة.** موظف عمل ريفريش عشرين مرة ماعملش حاجة.
- **حساب كل متصل لوحده.** غير كده أول مهاجم بيقفل العيادة كلها — يبقى الحاجة
  اللي بتمنع تعطيل الخدمة هي اللي عملته.
- **الجدول بيتكنس.** حد بيلف على عناوين كان هيملا الذاكرة لحد ما البرنامج
  يموت.

> **وقد إيه ده بيغطّي بصراحة:** نافذة ثابتة في ذاكرة العملية الواحدة. بتتصفّر
> مع كل إعادة تشغيل، ولو في أكتر من عملية كل واحدة بتعدّ لوحدها. ده حجمه
> الحقيقي: برنامج عيادة واحدة على عملية واحدة، والخطر هو سكريبت بيدق على
> فورم الدخول. عيادة هتفتح ده على الإنترنت المفتوح محتاجة limiter حقيقي
> (Redis) أو بروكسي قدّامه — **وده مش بديل عن أي منهم**.

### 6) النسخة الاحتياطية فيها كل بيانات المرضى — من غير تشفير

النسخة بقت `.zip` فيه قاعدة البيانات **وصور المرضى والمستندات**. لو اتنسخت
على فلاشة أو اتبعتت على واتساب، دي كل بيانات العيادة.

**الإصلاح:** خيار **كلمة سر للنسخة** (zip مشفّر أو `age`/GPG)، وتحذير مكتوب
عند التنزيل.

### 7) كوكي «افتكرني» — ✅ اتعمل

`SESSION_COOKIE_SECURE` بقى بيتحدد وقت بناء التطبيق من `HTTPS=1` في
`clinic.env` (اتعمل مع بند ١). الفاضل كان كوكي «افتكرني»:
`REMEMBER_COOKIE_HTTPONLY` كان في `ProductionConfig` **بس** — يعني عيادة
شغالة على الإعداد الافتراضي كان كوكي «افتكرني» بتاعها مقروء لأي سكريبت في
الصفحة.

اتنقل للكلاس الأساسي مع `REMEMBER_COOKIE_SAMESITE`. **وده الكوكي الأهم في
الاتنين:** بيدخّلك من غير الجلسة، وعمره أطول منها.

### 8) رفع الملفات من داخل البرنامج — ✅ اتعمل

`wa_media` (الوارد من واتساب) كان متشدّد أصلاً. `save_document`
و`save_drug_media` (الرفع من داخل الشاشات) بقوا زيه:

- **النوع بيتحدد من البايتات مش من الاسم.** المجلد ده بيتقدّم من نفس أصل
  البرنامج، فاللي بيسمّي الملف مالوش حق يقرّر المتصفح هيعامله كإيه. ملف
  بايتاته مش من الأنواع اللي العيادة بتحفظها بيترفض، مابيتحفظش تحت الامتداد
  اللي اسمه ادّعاه.
- **مفيش SVG ولا HTML في اللستة** — الاتنين بيشيلوا سكريبت، وسكريبت بيتقدّم
  من أصل العيادة بيشتغل بجلسة اللي فتحه.
- **سقف ٢٠ ميجا** — رفعة واحدة تملا هارد العيادة مش تسجيل نتيجة.
- **و`X-Content-Type-Options: nosniff`** على كل رد، عشان المتصفح مايراجعش
  النوع اللي بعتناه ويعيد تفسير أشعة كصفحة. ومعاها `X-Frame-Options` و
  `Referrer-Policy`.

---

## 🟢 كويس فعلاً — متسجّل علشان مايتكسرش

| البند | الحالة |
|---|---|
| الباسوردات | `werkzeug.generate_password_hash` (مش MD5/SHA خام) ✅ |
| SQL Injection | كل الاستعلامات عبر SQLAlchemy ORM — مفيش SQL نصّي بمدخلات ✅ |
| قفل الدخول | ٥ محاولات / ١٥ دقيقة لكل حساب، **وسقف لكل متصل فوقه** ✅ |
| رفع الملفات | النوع من البايتات مش من الاسم، وسقف ٢٠ ميجا ✅ |
| هيدرات الرد | `nosniff` · `X-Frame-Options` · `Referrer-Policy` ✅ |
| `_is_safe_next` | بيمنع إعادة التوجيه لموقع خارجي بعد الدخول ✅ |
| الصلاحيات | `module_required` + `capability_required` على كل مسار، والاستقبال بيحصّل من غير ما يشوف الحسابات ✅ |
| سجل التدقيق | `ActivityLog` على كل عملية مؤثّرة ✅ |
| حقن الأوامر في الـ AI | `AI_SYSTEM` متشدّد، والوارد بيتسلّم **كبيانات** مش كتعليمات ✅ |
| الفترات المحاسبية | الشهر المقفول بيرفض الفلوس **وحركات المخزن** ✅ |
| مسار الملفات في الاستعادة | فيه حارس ضد `../` (path escape) ✅ |
| الذكاء الاصطناعي المحلي | خيار Ollama/LM Studio — البيانات ماتخرجش من العيادة ✅ |

---

## الترتيب المقترح للتنفيذ

| # | البند | الحجم | الأثر |
|---|---|---|---|
| 1 | SECRET_KEY يتولّد ويترفض الافتراضي | صغير | يقفل تزوير الجلسات |
| 2 | `FLASK_CONFIG=production` + waitress | صغير | يقفل كونسول الديباج |
| 3 | توقيع webhook بتاع Meta | صغير | يقفل حقن الوارد |
| 4 | CSRF على كل الفورمات | **متوسط** (كل القوالب) | يقفل التزوير من بره |
| ~~5~~ | ~~Rate limiting~~ | ✅ اتعمل | |
| 6 | **تشفير النسخة الاحتياطية** | متوسط | يحمي البيانات بره الجهاز |

**الفاضل بند واحد: ٦.** النسخة `.zip` فيها قاعدة البيانات وصور المرضى
والمستندات — نُسخت على فلاشة أو اتبعتت على واتساب تبقى دي كل بيانات العيادة
ماشية من غير قفل.

---

> **ملاحظة على المنهج:** المراجعة دي على الكود المدموج في `main` لحد
> PR #208. مافيهاش فحص اختراق فعلي — دي قراءة كود ومسارات، والبنود الحرجة
> اتأكّدت بقراءة الملفات المذكورة سطر بسطر.

---

## ✅ اللي اتقفل

| البند | اللي اتعمل |
|---|---|
| **١ — وضع الديباج** | `run.py` بقى `production` افتراضياً، والخدمة بـ **waitress** مش خادم التطوير. المطوّر بيطلب الديباج صراحة: `FLASK_CONFIG=development` |
| **٢ — SECRET_KEY** | بيتولّد عشوائي (٦٤ حرف) ويتكتب في `clinic.env` أول تشغيل — **وللتنصيبات القديمة عند التحديث كمان**، لأنهم بالظبط المكشوفين |
| **٤ — توقيع الـ webhook** | مِيتا: `X-Hub-Signature-256` بـ HMAC-SHA256 على **البايتات الخام** ومقارنة `compare_digest`. WaPilot: نفس المقارنة الآمنة على توكن المسار + حد أدنى لطوله. **الاتنين بيرفضوا وهم مش متظبطين** |
| **٣ — CSRF** | `CSRFProtect` متسجّل، و**٢١٨ فورم** في ٧٧ قالب شايلين توكن، و`base.html` بيضيفه كهيدر للطلبات اللي بتتبعت بسكربت. الـ webhooks مستثناة لأنها بتثبت نفسها بالتوقيع. وفحص ستاتيكي بيقرا كل القوالب فأي فورم جديد من غير توكن بيسقط في الاختبارات |

### وحاجة تالتة اتكشفت وهي بتتعمل

`clinic.env` **ماكانش بيوصل للإعدادات أصلاً.** كلاسات `config` بتقرا
`os.environ` وهي بتتعرّف — يعني وقت `import` — و`run.py` بيستورد التطبيق
**فوق** وبيقرا `clinic.env` **تحت**. فكل حاجة العيادة كتباها في الملف كانت
بتتقرا متأخر وتترمي بالساكت: **مكان قاعدة البيانات** و**مفتاح الجلسات**
كمان.

يعني توليد المفتاح كان هيبقى بلا فايدة — التطبيق كان هيفضل يوقّع بالمفتاح
المنشور في الكود.

الإصلاح: القراءة بقت جوّه `create_app` وقت بناء التطبيق مش وقت الاستيراد.

### ملاحظة على الكوكيز

`SESSION_COOKIE_SECURE` **مش** مفعّلة افتراضياً عن قصد: معظم العيادات بتشغّل
البرنامج على شبكة العيادة بـ HTTP عادي، وتعليم الكوكي «آمن» من غير شهادة
معناه إن المتصفح مابيبعتهاش تاني و**محدش يقدر يدخل خالص**. اللي عنده HTTPS
فعلاً بيحطّ `HTTPS=1` في `clinic.env` والكوكيز بتتشدّ معاها.
