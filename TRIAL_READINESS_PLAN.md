# 🚦 خطة الاستعداد للتشغيل التجريبي

> كل الملاحظات والباجات اللي طلعت من مراجعة الشاشات، مجمّعة ومقسّمة على دفعات
> نقدر نراجع كل واحدة لوحدها.
> يوليو 2026 — الخطة اتكتبت بعد مراجعة ١٩ ملاحظة على السيستم.

---

## 🧭 إزاي الخطة مقسومة، وليه

التقسيم **مش** بالشاشة ولا بالموديول. التقسيم بـ**نوع الضرر**:

| الدفعة | النوع | ليه في المكان ده |
|---|---|---|
| **١** | بتطلع **رقم أو بيان غلط** | العيادة تقدر تشتغل بشاشة وحشة. مش تقدر تشتغل بفاتورة غلط أو ميعاد جرعة غلط. دي اللي بتمنع التشغيل التجريبي فعلاً. |
| **٢** | **بتمنع استخدام** ميزة موجودة | الكود موجود والشاشة موجودة، وحاجة صغيرة ناقصة بتقفل الباب. أرخص إصلاح وأعلى مكسب. |
| **٣** | **إعادة تصميم** | الوظيفة صح والشكل بيتعب المستخدم. مهم، ومش بيوقف تشغيل. |
| **٤** | **إضافات تشغيلية** | بتوفّر وقت على الاستقبال والدكتور. بعد ما السيستم يبقى صح. |

**وقاعدة واحدة ماشية في الأربعة:** أي بند بيغيّر رقم بيتعمل معاه اختبار
يفشل قبل الإصلاح وينجح بعده. ومش بنعيد تصميم شاشة قبل ما الرقم اللي تحتها
يبقى صح — شاشة حلوة بترقم غلط هي أخطر من شاشة وحشة.

---

## ✅ اللي اتأكّدت منه في الكود بنفسي

الجزء ده مهم: أنا مافترضتش من الوصف، فتحت الكود وشغّلت السيناريو.

| # | البند | الحالة | المكان |
|---|---|---|---|
| ١٦ | جرعة ٢ ميعادها قبل جرعة ١ | 🔴 **متأكد — أعدت إنتاجها** | `app/utils/vaccines.py:359` |
| ١١ | ١١ تطعيم مقترح من غير قاعدة | 🟠 **عرض جانبي للباج ١٦** | `app/utils/vaccines.py:739` |
| ٩ | اسم ولي الأمر بلغتين | 🔴 **متأكد** | `app/utils/consent.py:93` |
| ٥ | مافيش قوالب قياسات للأجهزة | 🔴 **متأكد** | `app/utils/reference.py:71` |
| ١٩ | الخصم بياخد ولي أمر واحد بس | 🔴 **متأكد** | `app/models/patient.py:177` |
| ١٣ | سعر التطعيم متلخبط مع رسمه | 🔴 **متأكد جزئياً** | `app/blueprints/finance/routes.py:1409` |
| ٣ | «مافيش شاشة عقود» | 🟢 **الشاشة موجودة** | `/finance/payers` |

### تفاصيل كل واحدة

#### ١٦ — جرعة ٢ ميعادها قبل جرعة ١ اتأخدت 🔴

ده اللي شغّلته: طفل مولود ٢٠٢٥-٠١-٠١، تطعيم جرعاته على ٢ و٤ و٦ شهور،
والجرعة الأولى اتأخدت متأخرة في ٢٠٢٦-٠٧-٢٩.

```
dose 1: given=2026-07-29   due=2025-03-01   status=done
dose 2: given=None         due=2025-05-01   status=overdue   ← قبل جرعة ١ بـ١٥ شهر
dose 3: given=None         due=2025-07-01   status=overdue
```

**السبب:** في `patient_plan` فيه كود بيربط الجرعات ببعض:

```python
min_iv = vaccine.min_interval_days
...
if due and prev_date and min_iv:          # ← min_iv = None
    earliest = prev_date + timedelta(days=min_iv)
```

و`min_interval_days` **فاضي (`NULL`) في كل التطعيمات تقريباً** — الحقل موجود
والـseeding بيحطّه لما يلاقيه في المصدر بس. فالشرط عمره ما بيتحقّق، والربط
عمره ما بيجري، وكل جرعة بتفضل على تاريخ عمرها الخام مهما حصل في الواقع.

**الإصلاح:**
1. `min_iv or _CATCH_UP_MIN_INTERVAL` — القيمة ٢٨ يوم موجودة أصلاً في الملف
   (`_CATCH_UP_MIN_INTERVAL`) ومستعملة في الجدول التعويضي، فالبرنامج **عارف**
   الرقم ده وبس مش بيستعمله في الحسبة الأهم.
2. وأرضية صلبة فوق كده: **جرعة عمرها ما تستحق قبل الجرعة اللي قبلها** — سواء
   فيه فاصل مسجّل أو مافيش. ده بيمسك الحالة اللي مافيش فيها أي بيانات فاصل.

#### ١١ — «على أي أساس ١١ تطعيم مقترح؟» 🟠

**فيه قاعدة، والقاعدة مكسورة بالباج ١٦.** القاعدة (`visit_vaccine_panel`):
تطعيم اختياري + مش `on_demand` + أول جرعة حالتها `due` أو `overdue`.

بس بما إن الباج ١٦ بيسيب كل الجرعات على تواريخ عمرها الخام، فأي طفل عدّى
سنّه الجدول → **كل جرعة في كل تطعيم اختياري بتبقى `overdue`** → كلهم
بيتقترحوا. ومن هنا الـ١١.

> **يعني ١١ مش بند لوحده — هو نفس الباج ١٦.** إصلاح ١٦ بيرجّع الاقتراحات
> لعددها الصح لوحده. ولما نصلّحه هنعدّ الاقتراحات لنفس الطفل قبل وبعد
> ونكتبه في الاختبار.

#### ٩ — اسم ولي الأمر عربي في الزيارة وإنجليزي في الملف 🔴

طريقين لنفس المعلومة، واحد بيعرف اللغة وواحد لأ:

```python
# app/utils/consent.py:93 — شاشة الزيارة
"name": guardian.full_name or "",              # عربي دايماً

# app/templates/patients/profile.html:274 — ملف المريض
value="{{ g.display_name(current_lang) }}"     # إنجليزي لو اللغة إنجليزي
```

**الإصلاح:** `default_guardian` تاخد `lang` وتستعمل `display_name(lang)`.
والأهم من الإصلاح نفسه: **ندوّر على باقي الأماكن اللي بتقرا `full_name`
مباشرة** بدل `display_name` — الباج ده نوعه بيتكرّر.

#### ٥ — الأجهزة مزروعة، وقوالب قياساتها لأ 🔴

البرنامج بيزرع كتالوج الأجهزة (`_devices()`)، وبيزرع **خدمة بسعر لكل جهاز**
(`_device_services()`) — وبعدين مافيش ولا سطر `DeviceMeasurement` بيتعمل في
أي حتة في الكود كله. دوّرت.

يعني الجهاز بيوصل للعيادة **مزروع ومسعّر ومقفول**: تفتح
`/visits/studies/new/...?device_id=1` تلاقي «الجهاز ده مالوش قالب قياسات».

**الإصلاح:** قوالب قياسات افتراضية لكل `device_type` مزروع — spirometry
(FEV1, FVC, FEV1/FVC, PEF) · ecg (rate, rhythm, PR, QRS, QT) · echo (EF,
LVEDD, valves) · audiometry · وهكذا — **قابلة للتعديل** ومتعلّمة `is_seeded`
زي قوالب التطعيم، عشان تعديل الدكتور عمره ما يتمسح.

> ومنطق «ازرع اللي ناقص، ماتلمسش اللي موجود» متعمول قبل كده مرتين في
> البرنامج (`seed_vaccine_schedules` و`seed_accounts`) — بناخد نفس الشكل.

#### ١٩ — الخصم بياخد ولي أمر واحد بس 🔴

```python
# app/models/patient.py:177
@property
def client_category(self):
    parents = sorted(self.family.parents,
                     key=lambda p: (0 if p.is_primary_contact else 1))
    return parents[0].client_category if parents else "normal"
```

**بياخد جهة الاتصال الأساسية وبس.** حالتك بالظبط: الأم «صديق» وهي جهة
الاتصال، والأب «موظف» → البرنامج بيقول «صديق» ويرمي إن الأب موظف.

**والإصلاح مش «خُد الفئة الأعلى»** — الفئات مالهاش ترتيب، ومين أحسن «صديق»
ولا «موظف» ده **قرار العيادة** ومكتوب في قيمة الخصم نفسها مش في الكود.

فالإصلاح: الفئات تبقى **مجموعة** (كل أولياء الأمور)، وقواعد الخصم تتحسب
على كل واحدة فيهم، **وأكبر خصم ناتج هو اللي يتطبّق**. كده احترمنا تسعير
العيادة بدل ما نخترع ترتيب من عندنا.

#### ١٣ — سعر التطعيم مع رسم التطعيم 🔴

التصميم في الكود **صح في النية**: سطر للتطعيم بسعر الـbrand
(`no_commission` عشان الدكتور ماياخدش عمولة مرتين) + سطر للرسم مرة واحدة بس.

**بس الاتنين ماخدين نفس `service_id`:**

```python
sid = str(vac_svc.id) if vac_svc else ""      # خدمة «رسم تطعيم»
...
lines.append({"service_id": sid,              # ← سطر التطعيم نفسه
              "unit_price": b.price or 0, ...})
```

يعني **سطر سعر التطعيم متعلّق بخدمة «رسم تطعيم»**. ودي بالظبط اللي انت
وصفتها: أي خصم مركّب على `category == "vaccination_fee"` بينزل على سعر
التطعيم كمان، والواحد يقدر يخصم الرسم ويحاسب سعر التطعيم.

**الإصلاح:** التطعيم ياخد هوية بنده الحقيقية (`vaccine_brand_id` موجود على
`InvoiceItem` أصلاً) وميبقاش متعلّق بخدمة الرسم.

**والنص التاني من الملاحظة** — «الفاتورة ناقصة سطر التطعيم» — محتاج نشوف
الفاتورة اللي حصل فيها كده: `_vaccine_prefill_lines` بيجري على
`_uncharged_vaccines` وقت التحصيل، ولو الجرعة اتسجّلت بعد ما الفاتورة
اتعملت، مسار التحصيل اللي انت مشيت فيه يمكن مايكونش عدّى على الprefill.
**هستنى أشوف الحالة أو أعيد إنتاجها قبل ما أدّعي سبب.**

#### ٣ — شاشة العقود 🟢 موجودة

مالقيتهاش لأنها **مش تحت الإعدادات**. هي تحت **المالية ← الجهات**:

| الشاشة | بتعمل إيه |
|---|---|
| `/finance/payers` | الجهات المتعاقدة (تأمين، شركات، نقابات) |
| `/finance/payers/<id>/rates` | **سعر كل خدمة في العقد** — ده اللي بتسعّر منه |
| `/finance/payers/<id>/contract/new` | عقد جديد |
| `/finance/payers/<id>/members` | المشتركين |
| `/finance/claims/<id>` | المطالبات والتحصيل |

**وإجابة سؤال «أسعّر منين؟»** — على مستويين:
1. **السعر الأساسي** على الخدمة نفسها في `/settings/services`، والتطعيم على
   الـbrand.
2. **سعر العقد** في `/finance/payers/<id>/rates` **بيطغى** على الأساسي لمريض
   مغطّى بالجهة دي.

> **فالبند ده مش «ابنِ شاشة»، هو «الشاشة مش بتتلاقى».** وبناء تانية كان
> هيعمل مكانين للعقود ولا واحد فيهم كامل. الإصلاح في الدفعة ٢ (تنقّل)،
> مش بناء جديد.

---

## 🔴 الدفعة ١ — اللي بيطلع رقم أو بيان غلط

**دي اللي لازم تخلص قبل أي مريض حقيقي.** كلها متأكد منها فوق.

| # | البند | الأثر |
|---|---|---|
| ١٦ | ربط مواعيد الجرعات | ميعاد جرعة قبل الجرعة اللي قبلها — والأهل بياخدوا ميعاد غلط |
| ١١ | الاقتراحات (نفس ١٦) | ١١ اقتراح بدل اللي يستحق فعلاً |
| ١٣ | هوية بند التطعيم في الفاتورة | خصم الرسم بينزل على سعر التطعيم |
| ١٩ | الخصم بأكبر استحقاق في الأسرة | العيادة بتاخد من عميل مستحق خصم أكبر |
| ٩ | اسم ولي الأمر بلغة الواجهة | اسم مختلف في شاشتين لنفس الشخص، وفي إقرار موقّع |

**ترتيب الشغل:** ١٦ (ومعاه ١١) → ١٣ → ١٩ → ٩.
السبب: ١٦ بيحل بندين، و١٣ و١٩ بيمسّوا فلوس، و٩ أصغرهم وأوضحهم.

**كل واحد PR لوحده** ومعاه الاختبار اللي بيفشل قبل الإصلاح.

---

## 🟠 الدفعة ٢ — حاجة صغيرة بتقفل باب مفتوح

| # | البند | الناقص بالظبط |
|---|---|---|
| ٥ | قوالب قياسات الأجهزة | زرع قوالب افتراضية `is_seeded` لكل `device_type` |
| ٣ | شاشة العقود مش بتتلاقى | لينك من الإعدادات ومن تسعير الخدمات ← `/finance/payers` |
| ٧ | ملف المريض مش بيوري اللي اتعمل | الإقرارات ودراسات الأجهزة موجودة في الزيارة ومش في الملف |
| ١٨ | لستة تليفونات ناقصة | الـ١٣ حالة دلوقتي رقم في تنبيه — عايزين شاشة الاستقبال يكلّم منها ويضيف |
| ٢ | شاشة أسعار أنواع الزيارات | **سؤال ليك تحت** — محتاج أعرف قصدك قبل ما أعدّل |

---

## 🟡 الدفعة ٣ — إعادة تصميم

الأربعة الأولانيين **نفس مشكلة التصميم**: كروت مضغوطة بتتحوّل لعجينة لما
البنود تكتر. فبيتعملوا بنفس النمط الجديد، مش أربع تصميمات.

| # | الشاشة | المطلوب |
|---|---|---|
| ١ | `/settings/services` | نمط جديد + **إضافة أنواع خدمات** (مش موجودة) + بحث وتصفية عشان تفضل شغالة مع ٢٠٠ خدمة |
| ٤ | `/settings/devices` + البوب-أب | نفس النمط |
| ١٤ | `/settings/data` قسم التصدير | نفس النمط |
| ٨ | البحث في الأدوية | **أكبر واحدة في الدفعة دي** — إعادة تصميم كاملة |
| ٦ | شهادة التطعيمات | تجميع بالتطعيم زي مرجع مركز الإسكندرية: كارت لكل تطعيم · صفوف الجرعات بتاريخ ودكتور وسجل · شريط تقدّم (١/٢، ٤/٤) · عدّادات فوق (سنين / أنواع / إجمالي) · والجدول جوّه الشهادة |
| ١٥ | صياغة الإقرارات | نص خاص لكل نوع إقرار، واضح بالعربي والإنجليزي حسب اللغة الشغالة |

**والنمط الجديد بيتعمل مرة واحدة** ويتطبّق على الأربعة — عشان الملاحظة
اللي قلتها بنفسك: «طبّقه في أي حتة النمط ده متكرر فيها».

---

## 🔵 الدفعة ٤ — تشغيل وسرعة

| # | البند | ملاحظة |
|---|---|---|
| ١٢ | شاشة التحصيل | اسم المريض والدكتور يبانوا بعد «تحصيل» · مايختارش من الأول والمريض واقف · شاشة الدفع المتعدد تبقى هي اللي تظهر · رسالة «افتح الوردية» تبقى واضحة |
| ١٠ | الإجراءات ودراسات الأجهزة | اختيار الجهاز في الاتنين بيلخبط. **قرار مطلوب تحت.** |
| ١٧ | مافيش تحديث لحظي | تعديل الدكتور مايبانش للمدير غير بـrefresh. **أكبر بند تقني في الخطة كلها** |

### ١٧ — التحديث اللحظي: ليه آخر واحد

مش لأنه مش مهم — لأنه **مش باج، ده قدرة جديدة في كل الشاشات**، وبيتعمل
غلط بسهولة:

- **polling على كل شاشة** = حِمل دايم على السيرفر ومافيش لحظية حقيقية.
- **WebSockets** = لحظية حقيقية، وبتغيّر شكل تشغيل البرنامج (worker مختلف).
- **SSE + تحديث الجزء المتغيّر بس** = وسط، وأقرب لشكل البرنامج الحالي.

**والقرار ده مايتأخدش وإحنا بنصلّح باجات.** فبييجي بعد الدفعة ١ و٢ ما
يخلصوا، ومعاه اقتراح مكتوب بالتفصيل ونختار.

---

## ❓ محتاج قرار منك

الأربعة دول بيغيّروا شغل مختلف على حسب ردك، فمش هخمّن:

### ١) شاشة أسعار أنواع الزيارات — البند ٢
سألت «هي بتعمل إيه؟». هي دلوقتي بتربط **نوع الزيارة** (كشف/استشارة/إعادة/
تطعيم) بـ**خدمة** بسعر — وده اللي بيخلّي «حجز كشف» يتحوّل لسطر بسعره في
الفاتورة لوحده. السؤال: تحبّها **تندمج جوّه شاشة الخدمات** (خانة «نوع
الزيارة» على الخدمة) ولا تفضل شاشة لوحدها؟

### ٢) الجهاز تحت الإجراءات ولا لوحده — البند ١٠
انت قلت الاختيار في الاتنين ملخبط، وسألت الجهاز يبقى فين. الاتنين ينفعوا:
- **جوّه الإجراءات**: الدكتور بيفكّر «هعمل إيه للطفل» — غيار، حقنة، إيكو،
  كله مكان واحد. واختيار الجهاز يفتح فورم القياسات.
- **لوحده**: الدراسة ليها قياسات وتقرير وطبقة بيانات مختلفة عن الغيار.

**أنا مايل للأول** (اختيار الجهاز من الإجراءات يفتح فورم الجهاز ويسجّله)،
عشان بيشيل الاختيار المكرّر اللي انت اشتكيت منه أصلاً. بس ده قرارك.

### ٣) شهادة التطعيمات — البند ٦
المرجع اللي بعتّه (مركز الإسكندرية + الـPDF) هياخد شكل الكارت وشريط التقدّم
والعدّادات. السؤال: **الجدول القادم يطبع مع الشهادة** ولا الشهادة تبقى
«اللي اتعمل» بس وورقة تانية للجاي؟

### ٤) التحديث اللحظي — البند ١٧
بعد ما الدفعة ١ و٢ يخلصوا هكتب اقتراح بالتلات طرق فوق وتختار. **ولو عندك
تفضيل من دلوقتي قوللي** عشان أحسبه في التصميم بدل ما أعيد.

---

## 📌 الحالة دلوقتي

| البند | الحالة |
|---|---|
| خطة الخزن (`TREASURY_PLAN.md`) | ✅ ٥ مراحل خلصت — PR #217 لـ #222 مدموجين، #223 على CI |
| الاختبارات | **٧١٧ ناجحة** · flake8 نضيف |
| الخطة دي | 📋 مكتوبة ومستنية مراجعتك |

**الخطوة اللي بعدها:** أبدأ الدفعة ١ من بند ١٦ (ومعاه ١١) — الاختبار اللي
بيعيد إنتاج الجرعة الغلط موجود عندي جاهز فوق.

> ومحتاج أقول حاجة بصراحة عن الترتيب: انت طلبت التشغيل التجريبي قريب، والدفعة
> ٣ (التصميم) هي اللي بتبان للعين أكتر. بس تشغيل تجريبي بشاشات حلوة بترقّم
> غلط بيخرّب الثقة في السيستم من أول أسبوع، ورجوعها أصعب بكتير من استحمال
> شاشة مضغوطة أسبوعين. عشان كده التصميم في ٣ مش في ١. **ولو محتاج العكس
> لأي سبب تشغيلي، ده قرارك وأنا أنفّذه.**
