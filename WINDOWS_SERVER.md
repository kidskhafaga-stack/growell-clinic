# 🖥️ تشغيل البرنامج على Windows Server

> **الهدف:** البرنامج يفضل شغال، ما يعلّقش، ولما السيرفر يتقفل ويترجع يفتح
> **يشتغل لوحده من غير ما حد يعمل لوج إن**.
> **المُختبَر عليه:** Windows Server 2012 R2 (بيشتغل على ويندوز ١٠/١١ برضه).

---

## 🚀 التركيب — تلات خطوات

**١)** شغّل `start.bat` **مرة واحدة بإيدك**. دي اللي بتعمل `.venv` وتنزّل
المكتبات وتجهّز قاعدة البيانات، ولازم تحصل وحد قاعد قدام الشاشة عشان لو
حاجة نقصت تبان.

**٢)** كليك يمين على `service.bat` ← **Run as administrator** ← اختار **[1]
Install**.

**٣)** لو فيه أجهزة تانية في العيادة هتفتح البرنامج، اختار **[6]** عشان يفتح
المنفذ في الفايروول.

خلاص. من دلوقتي البرنامج بيقوم مع السيرفر.

---

## 🧠 ليه Task Scheduler مش «سيرفس» حقيقية

`sc create` عايزة برنامج بيتكلم بروتوكول الـ Service Control Manager، و
`python.exe` ما بيعملش كده — فالسيرفس اللي تتعمل بالطريقة دي بتقوم وتتقتل
بعد ثواني بحجة «مش بيرد». الحل المشهور برنامج تاني (NSSM) يتنزّل من النت، و
تنزيل برنامج غريب على سيرفر عيادة ده مشكلة دعم فني بعدين.

الـ Task Scheduler **موجود جوّه** Server 2012 R2، وبيشغّل باسم `SYSTEM` —
مالوش باسورد يتخزن، وما بيتأثرش بتسجيل الخروج، **وبيقوم قبل ما حد يدخل** —
وبيعيد تشغيل التاسك اللي بتقع. بيعمل الشغلانة من غير أي حاجة تتنزّل.

---

## ⚙️ التلات إعدادات اللي هما الموضوع كله

كل واحدة فيهم كانت سبب إن «سيرفس» عند حد تقف في صمت:

| الإعداد | ليه |
|---|---|
| `ExecutionTimeLimit = PT0S` | **الافتراضي تلات أيام وبعدين الويندوز بيقتل التاسك.** سيرفر سايب شغال كان هيقع اليوم الرابع، في نفس الساعة اللي اتشغّل فيها، من غير أي سطر في اللوج يفسّر. |
| `RestartOnFailure` كل دقيقة × ٩٩٩٩ | وقعة الساعة ٢ بالليل لازم ما تحتاجش حد. والعدد الصغير معناه إنه بيستسلم في الليلة اللي محتاجينه فيها. |
| `BootTrigger` + `SYSTEM` | ده بالظبط معنى «يرجع من غير ما حد يعمل لوج إن». |

ومعاهم: `StopIfGoingOnBatteries=false` — التاسك اللي بيرفض يشتغل لأن الـUPS
بيقول «على البطارية» هو تاسك بيفضل واقع بعد بالظبط الحدث اللي الـUPS
اتشترى عشانه.

---

## 🐕 والووتش-دوج: «شغال» مش معناها «بيرد»

الـScheduler بيرجّع البرنامج اللي **وقع**. لكن العطل اللي العيادة بتقابله
فعلاً هو التاني: البرنامج **شغال** وما بيردّش — قاعدة بيانات مقفولة، أو
خيوط خلصت. من برّه ده شكله زي الصحة بالظبط، والتاسك بيقول Running وانت
واقف مش قادر تحجز مريض.

فالتاسك التاني بيسأل البرنامج نفسه على `/healthz` كل ٥ دقايق. والراوت ده
**بيلمس قاعدة البيانات بقصد** — فحص بيثبت إن بايثون عايش بس كان هيرد بمنتهى
السعادة وسط بالظبط العطل اللي هو موجود عشانه.

وبيسأل **مرتين** قبل ما يعمل حاجة: رد بطيء واحد وقت نسخة احتياطية مش سيرفر
ميت، وووتش-دوج بيعمل ريستارت مع كل عثرة أوحش من إنه مايبقاش موجود.

> **ليه بايثون مش سطر batch؟** Server 2012 R2 مافيهاش `curl.exe` (دي جت مع
> ويندوز ١٠ إصدار 1803)، و`Invoke-WebRequest` على PowerShell 4.0 اللي جاي
> معاها بتطلب إعدادات أول تشغيل لإنترنت إكسبلورر قبل ما تجيب أي حاجة — يعني
> على سيرفر جديد بتفشل لسبب مالوش علاقة بالبرنامج.

---

## 📋 القائمة في `service.bat`

| | |
|---|---|
| **[1] Install** | بيسجّل التاسكين ويشغّل البرنامج على طول (من غير ريستارت). |
| **[2] Remove** | بيوقّف ويشيل التسجيل. **بياناتك ما بتتلمسش.** |
| **[3] Status** | مسجّل ولا لأ، وآخر نتيجة — **وكمان بيسأله فعلاً هو بيرد ولا لأ**. |
| **[4] / [5]** | تشغيل / إيقاف دلوقتي. |
| **[6] Firewall** | بيفتح المنفذ اللي البرنامج شغال عليه فعلاً — بيقراه من `clinic.env`، مش رقم مكتوب في الملف. قاعدة فايروول لـ5000 والبرنامج شغال على 8080 هي صباح ضايع في «الأجهزة التانية مش شايفاه». |

---

## 📄 اللوجات

| الملف | فيه إيه |
|---|---|
| `logs\service.log` | كل تشغيلة: وقت البدء، ترقية الشكل، وأي كراش بكوده. بيتقلّب لوحده عند ٢٠ ميجا لـ`service.old.log` — لوج محدّش بيقلّبه هو ديسك بيمتلي، والديسك المليان بيوقّف العيادة زي الكراش بالظبط. |
| `logs\watchdog.log` | بس لما البرنامج ما يردّش. صامت طول ما كل حاجة تمام. |

---

## 🔧 لو حاجة مامشيتش

**«التاسك موجود بس البرنامج مش شغال»** → افتح `logs\service.log`. أشهر سببين:
`.venv` مش موجود (شغّل `start.bat` بإيدك مرة)، أو `sync-db` فشل — وفي الحالة
دي البرنامج **بيرفض يشتغل بقصد**: قراءة قاعدة بيانات شكلها مش مطابق للكود
بتطلّع أرقام غلط مش رسالة خطأ.

**«شغال عندي وباقي الأجهزة مش شايفاه»** → خيار [6]، وبعدين افتح من الأجهزة
`http://<اسم-السيرفر>:<المنفذ>`.

**«عايز أغيّر المنفذ»** → عدّل `PORT` في `clinic.env`، وبعدين [5] ← [4]،
وبعدين [6] تاني عشان قاعدة الفايروول تتظبط على الرقم الجديد.

**التحديثات** لسه بإيدك بـ`update.bat` — وده مقصود. التحديث التلقائي على
سيرفر عيادة معناه إن الكود يتغيّر في نص يوم شغل من غير ما حد قرّر، وده
بالظبط اللي `UPGRADE_PLAN.md` موجود عشانه. أوقف بـ[5]، شغّل `update.bat`،
وبعدين [4].
