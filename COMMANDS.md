# أوامر التشغيل والصيانة — GROWELL CLINIC

كل الأوامر دي موجودة كمان في قائمة رقمية جاهزة: **`tools.bat`** على ويندوز،
**`./tools.sh`** على لينكس/ماك. الملف ده للي عايز يفهم كل أمر بيعمل إيه
وإمتى يحتاجه.

> على ويندوز الأوامر بتتكتب من نفس فولدر البرنامج بعد ما تشغّل البيئة:
> `.venv\Scripts\activate.bat`
> وعلى لينكس/ماك: `source .venv/bin/activate`

---

## تغيير المنفذ (البورت)

البرنامج بيفتح على `5000` افتراضياً. لو المنفذ ده محجوز لبرنامج تاني على
الجهاز — وده بيحصل كتير — غيّره بأي طريقة من التلاتة:

| الطريقة | إزاي |
|---|---|
| **الدائمة** | افتح ملف `clinic.env` بالمفكرة وغيّر `PORT=5000` لـ `PORT=8080` |
| مرة واحدة | `start.bat 8080` &nbsp;/&nbsp; `./start.sh 8080` |
| للمطوّر | `python run.py 8080` |

ملف `clinic.env` بيتعمل لوحده أول مرة تشغّل البرنامج، وجنبه `start.bat`.
بعد أي تغيير فيه **اقفل البرنامج وافتحه تاني**.

لو المنفذ مشغول، البرنامج بيقول لك بالنص إنه مشغول ويقترح عليك تغيّره —
مش بيرمي رسالة خطأ مبهمة.

---

## بعد كل تحديث

```bash
flask --app run upgrade-db
```

**ده أهم أمر في الملف.** بيضيف الأعمدة والجداول الجديدة اللي التحديث جابها،
**من غير ما يلمس أي بيانات موجودة**، وبياخد نسخة احتياطية قبلها. آمن إنك
تشغّله أكتر من مرة — لو مفيش جديد بيقول كده ويقف.

`start.bat` و`start.sh` بيشغّلوه **في كل مرة** تلقائياً، فغالباً مش هتحتاج
تكتبه بإيدك. لو فشل، البرنامج بيوقف وبيقول لك — **ما تستخدمش البرنامج قبل
ما تحلّها**، وبياناتك زي ما هي.

```bash
flask --app run seed-reference
```

بيحدّث كتالوجات العيادة: التطعيمات، الخدمات الأساسية، المرجع الدوائي،
الفحوصات، مستلزمات المخزن، قوالب الرسايل. **بيملى الناقص بس** — أي حاجة إنت
عدّلتها بتفضل زي ما هي.

---

## النسخ الاحتياطي

النسخة بقت **ملف `.zip` فيه قاعدة البيانات والصور مع بعض** — صور المرضى
والموظفين وشعار العيادة وتوقيعات الأطباء والمستندات المرفقة.

- **من داخل البرنامج:** الإعدادات ← البيانات ← نسخة احتياطية
- **من `tools.bat`:** اختيار ٣ (أخذ نسخة) و٤ (عرض النسخ)

النسخ بتتحفظ في `instance/backups/`. البرنامج بياخد نسخة تلقائية يومياً،
ونسخة قبل أي ترقية أو استرجاع.

**الاسترجاع** بيتعمل من داخل البرنامج (الإعدادات ← البيانات) لأنه بياخد
نسخة من الوضع الحالي الأول — يعني الاسترجاع الغلط نفسه ليه رجعة.

---

## البيانات

```bash
flask --app run import-drugs <ملف.csv> --dry-run    # اقرا وقول، من غير حفظ
flask --app run import-drugs <ملف.csv>              # استورد فعلاً
```

بيقبل CSV و JSON، وبيفهم أعمدة سجل الأدوية المصري زي ما هي. للملفات الكبيرة
(٢٥ ألف صنف) **استخدم السطر ده مش المتصفح** — أسرع وما بيقعش في نص الطريق.
إعادة استيراد نفس الملف = صفر تغيير.

```bash
flask --app run seed-drugbook     # تحميل المرجع الدوائي من الأول
flask --app run seed-demo         # بيانات تجريبية للعرض (مرضى وزيارات وهميين)
flask --app run reset-data        # مسح بيانات التشغيل (بيحتفظ بالمستخدمين والكتالوجات)
flask --app run archive-inactive  # أرشفة ملفات المرضى الساكتة من زمان
```

⚠️ `reset-data` بيمسح المرضى والزيارات والفواتير. **خد نسخة احتياطية الأول** —
`tools.bat` بياخدها لوحده قبل ما ينفّذه.

---

## الحسابات

```bash
flask --app run create-admin
```

بيسألك على اسم المستخدم وكلمة المرور. محتاجه لو ضاعت كلمة مرور المدير الوحيد.

---

## أوامر أول تثبيت

```bash
flask --app run init-db     # إنشاء الجداول فاضية
flask --app run seed        # تثبيت أول مرة: الكتالوجات + حساب دخول
```

`start.bat` بيشغّل `seed` لوحده لو قاعدة البيانات مش موجودة، فمش هتحتاجهم
غالباً.

---

## الرسايل

```bash
flask --app run send-due
```

بيبعت رسايل الواتساب المجدولة اللي جه وقتها. البرنامج بيعمل ده لوحده وهو
شغال؛ الأمر ده للي عايز يشغّله من مهمة مجدولة (Task Scheduler / cron).

---

## نقل البرنامج لجهاز جديد

1. خد **نسخة احتياطية** من الجهاز القديم ونزّلها (الإعدادات ← البيانات ← تنزيل)
2. انسخ فولدر البرنامج على الجهاز الجديد (أو اعمل `git clone`)
3. شغّل `start.bat` — هيعمل كل حاجة لوحده
4. ادخل بحساب المدير ← الإعدادات ← البيانات ← **ارفع النسخة** ← استرجع

النسخة فيها الصور، فالملفات بترجع مع البيانات.

---

## لما حاجة تقع

| المشكلة | الحل |
|---|---|
| «المنفذ مشغول» | غيّر `PORT` في `clinic.env`، أو `start.bat 8080` |
| فشل ترقية قاعدة البيانات | ما تستخدمش البرنامج. بياناتك سليمة — استرجع آخر نسخة `preupgrade` |
| صور مكسورة بعد استرجاع | النسخة دي كانت `.db` قديمة (من غير صور). النسخ الجديدة `.zip` وفيها الصور |
| نسيت كلمة مرور المدير | `flask --app run create-admin` |
| البرنامج بطيء بعد استيراد ٢٥ ألف دواء | طبيعي — البحث محدود بالنتائج، مش بيحمّل الكتالوج كله |
