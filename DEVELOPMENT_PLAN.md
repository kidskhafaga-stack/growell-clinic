# 🗺️ خطة التطوير القادمة — GROWELL CLINIC / PediaPro

> ملف متابعة المشروع: المكتمل · الناقص · الملاحظات والتحسينات المطلوبة.
> انقل هذا الملف لأي سيشن جديد لمتابعة التطوير.
> آخر تحديث: يونيو 2026.

---

## 📌 كيف تكمل العمل

- كل التطوير على فرع لكل ميزة ثم Pull Request ودمج في `main`.
- **ترقية قاعدة البيانات آمنة:** أي جداول/أعمدة جديدة تُطبّق تلقائياً عبر
  `flask --app run upgrade-db` (مربوط في `start.bat` / `start.sh`) — **بدون فقد بيانات**.
  عند إضافة عمود لجدول موجود: أضِفه لقائمة `additions` في `app/cli.py`.
- **بيانات تجريبية:** الإعدادات ← أدوات البيانات ← «تحميل البيانات التجريبية»
  (أو `flask seed-demo`). المسح: نفس الشاشة (منطقة الخطر، تكتب `DELETE`).
- **الكتالوجات سطر واحد منطق:** التطعيمات في `app/data/egypt_vaccines.json`،
  الأدوية في `app/utils/drugs.py` — تُزرع تلقائياً وidempotent.
- **الرسائل/CRM موحّدة:** كل الرسائل تمر عبر `app/utils/whatsapp.py`
  (`send` + `template_body`) وتُسجَّل في `MessageLog`. وضع واحد (يدوي/تلقائي)
  من إعداد `crm_mode`. القوالب كلها في `MessageTemplate` (الرسائل ← المناسبات).

---

## ✅ المكتمل (مدمج في main)

- **المرضى والأسر:** CRUD + رقم ملف يدوي/تلقائي + ملف بتبويبات + الأسرة/الأخوة +
  فئة العميل + **استيراد بمعاينة قبل الحفظ** + تلوين ذكر/أنثى.
- **التأمين/العضوية:** جهات دافعة + **جدول تغطية لكل خدمة** + كروت عضوية للمريض +
  **عقود بتواريخ سريان** + خصم تلقائي على الفاتورة + مطالبات مطبوعة.
- **المواعيد:** لوحة يومية بحالات ملوّنة + جداول عمل الأطباء + الدور (رقم/توقيت).
- **الزيارة:** علامات حيوية + تشخيص + ملخص مطبوع + زر إنشاء فاتورة.
- **النمو:** WHO + Percentile/Z + منحنى تفاعلي + **طباعة A4** (منحنى + جدول القياسات).
- **التطعيمات:** الكتالوج كامل (24 تطعيم/45 ماركة من ملف الـPDF) + ماركات + جداول +
  **طريقة الإعطاء** + مخزون (لوط/صلاحية/تنبيهات/FEFO) + شهادة + **إشعار تطعيم تلقائي**
  (الجرعة + الجرعة القادمة).
- **الأدوية والروشتات:** قاعدة أدوية + **Autocomplete** + تنبيه تفاعلات + أقصى جرعة +
  **قوالب طباعة مرنة** (أبيض/ورق جاهز، لكل طبيب، معاينة) + توقيع/ختم/ترخيص الطبيب.
- **المخزون والموردين:** مخزون تطعيمات + **مخزن عام كامل** (أصناف + حركات + جرد +
  تنبيهات إعادة الطلب) + موردين.
- **المالي:** الخدمات والعمولات (نسبة طبيب/عيادة) + الفواتير والدفعات (مدفوع/جزئي/آجل) +
  تعديل الفاتورة + **كشف حساب الطبيب** + مطالبات الخصم + **الفاتورة الإلكترونية ETA**
  (وضع ديمو + EGS/GS1 + طابور).
- **CRM/واتساب موحّد:** وضع واحد (يدوي/تلقائي) + سجل قوالب واحد لكل الأنواع +
  تأكيد المواعيد + جدول الطبيب + **المناسبات وأعياد الميلاد** + إشعار التطعيم + سجل واحد.
- **النظام:** أدوار وصلاحيات قابلة للتعديل + ملفات مستخدمين/أطباء + تخصيص الواجهة
  (فاتح/داكن + لون + خط + صفحة افتراضية) + التقارير (مالي/تشغيلي/مخزون) +
  تصنيف المرضى في الداشبورد (نوع + فئات عمرية) + الشعار + **اسم تجاري PediaPro**
  + حقوق ملكية + بيانات تجريبية + أداة مسح.

---

## ⏳ الناقص أو الجزئي (مرتّب بالأولوية المقترحة)

1. 💰 **المالي — المصاريف والميزانية و P&L:**
   - جدول مصاريف (إيجار/رواتب/كهرباء/مستلزمات) يدوي + متكرر شهري + فئات.
   - تقرير **P&L** (إيرادات − مصاريف = صافي) + ميزانية مقدّرة vs فعلي + تنبيه تجاوز.

2. 💊 **حساب جرعة الدواء من الوزن (mg/kg):**
   - حقل `dose_per_kg` + `max_per_kg` على `Drug`، وعند كتابة الروشتة:
     إدخال وزن الطفل → حساب الجرعة المقترحة تلقائياً (مع حد أقصى).

3. 📊 **داشبوردات إضافية:**
   - داشبورد مالي (إيرادات/غير مدفوع/P&L + رسوم).
   - **بحث علمي/تحليلي:** تحليل الحالات حسب التشخيص/العمر/الجنس + **تصدير Excel/CSV**.
   - أداء الموظفين (Staff Performance).

4. 🛒 **أوامر الشراء الكاملة:** طلب شراء → موافقة → استلام (GRN) → دخول المخزون،
   وربطها بالموردين والمصاريف. (حالياً: استلام/حركات مبسّطة فقط.)

5. 🔎 **بحث ICD-10/11 في التشخيص:** يوجد MCP tool للـICD-10 — يُدمج بحث بالكود/الاسم
   في شاشة التشخيص بدل الإدخال اليدوي.

6. 📈 **مراجع نمو إضافية:** CDC (0-20) و RCPCH (UK) و WHO 5-19 — حالياً WHO فقط.

7. 🏷️ **نظام الخصومات المسمّى:** خصم عيادة (حملة) / خصم طبيب / خصم فئة (أقارب/أصدقاء/
   موظفين) / خصم خاص — حالياً خصم سطر يدوي + تغطية تأمين فقط.

8. 🤖 **الذكاء الاصطناعي (AI):** مساعدة تشخيص + مساعد أثناء الفحص + اقتراح جرعات +
   تلخيص أبحاث. (موديول `ai` لسه Coming soon.)

9. 🧾 **تطعيمات — تحسينات الـPDF المتبقية (اختياري، إضافي بدون هدم):**
   - حقول إضافية (الأمراض المغطّاة، أدنى/أقصى عمر، بوستر، موسمي، فئات الخطر).
   - **قوالب جداول حسب عمر بداية الجرعة (A/B/C/D)** + فترات دنيا/قصوى (Catch-up).
   - **QR في الشهادة** + تحقق.
   - تذكيرات موسمية (إنفلونزا سنوي) + الجرعة القادمة الآلية.

---

## 🐞 ملاحظات وتحسينات مطلوبة (من الاستخدام)

### 1) نظام حجز المواعيد — يحتاج يبقى أذكى وأسهل ⭐
**المطلوب (تدفق الحجز المثالي):**
1. ابحث عن **المريض** (بالاسم/الرقم) أو أنشئه سريعاً.
2. اختر **الطبيب**.
3. النظام يعرض **المواعيد المتاحة فعلاً** لهذا الطبيب (حسب جدول عمله + المحجوز)
   كـ«خانات» قابلة للضغط — مش إدخال وقت يدوي.
4. اضغط الخانة المتاحة → تأكيد الحجز.
- إضافات مقترحة: منع التعارض، عرض أقرب موعد متاح، قائمة انتظار، بحث «أقرب طبيب متاح».
- الحالي: نموذج حجز يدوي (تاريخ/وقت) بدون عرض الفراغات — غير سهل.

### 2) السوبر-أدمن يظهر كأنه طبيب — سبب وإصلاح
- **السبب:** `app/utils/appointments.py` → `list_doctors()` بيرجّع
  `User.role.in_(["doctor", "admin"])` — فالأدمن يظهر ضمن الأطباء في المواعيد.
- **الإصلاح المقترح:** قصرها على `role == "doctor"` فقط، أو إضافة علم
  `is_practitioner` على المستخدم (للأدمن اللي بيكشف فعلاً في العيادات الفردية)
  وفلترة الأطباء بـ `role=="doctor" OR is_practitioner`.

---

## 🎯 الأولوية المقترحة للبدء

> **(تحسين حجز المواعيد)** + **(إصلاح ظهور الأدمن كطبيب)** أولاً لأنهم تجربة استخدام يومية،
> ثم **المصاريف/P&L** + **جرعة الدواء بالوزن**، ثم الباقي.
